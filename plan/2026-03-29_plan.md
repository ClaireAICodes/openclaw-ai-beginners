# Morning Plan - 2026-03-29

## Executive Summary
- **Total ideas:** 12
- **Prioritized:** 12
- **Quick wins (<4h):** 0 (but two can be done in ~6h each)
- **Estimated total effort:** ~219 hours (~28 days at 8h/day). Top 5 require ~80 hours (~10 days).
- **Estimated monthly cost:** $0 (all free tiers)
- **Resources needed:** 1 GitHub repo, 11 new OpenClaw skills, Google OAuth credentials, Duolingo credentials, AnkiConnect setup, optional Alpaca keys.

## Detailed Plan

### 1. Skill Health Monitor & Daily Report (GREEN)
**Impact:** High | **Effort:** 6h | **Confidence:** High

**Overview:** Daily check verifies all skills load correctly, required env vars exist, and cron jobs are scheduled. Sends Telegram alerts on failures, preventing silent downtime (e.g., recent Paragraph skill failure).

**Resource Requirements:**
- Free hosting: None (runs locally)
- New OpenClaw skill
- Master must provide: Telegram chat ID (already have 129156516)

**Prerequisites:** None

**Feasibility:** ✅ Can build autonomously

**Implementation Steps:**
1. Create skill directory; define health checks (manifest load, env vars, cron).
2. Implement scanners, report generator, Telegram alerts.
3. Test with intentionally broken skill; verify detection.
4. Deploy as daily cron (6 AM SGT); log to `logs/skill-health-YYYY-MM-DD.json`.
5. Monitor alerts; refine checks over time.

**Blockers & Mitigations:**
- False positives → add retry logic (2 consecutive failures).
- Performance → limit to critical skills or cache results.

**Success Criteria:**
- Report by 6:15 AM SGT; alerts within 5 min of failure; reduced silent incidents.

---

### 2. Gym Progress Tracker & Workout Logger (GREEN)
**Impact:** Medium | **Effort:** 6h | **Confidence:** High

**Overview:** Telegram bot for quick workout logging via natural language or buttons. Stores sessions in SQLite, calculates PRs, sends weekly summaries.

**Resource Requirements:** None (free).

**Prerequisites:** None

**Feasibility:** ✅ Autonomous

**Implementation:**
1. Design SQLite schema.
2. Implement `/log` (regex NLP), `/prs`, `/weekly`; optional voice logging.
3. Test parsing and PR calculation.
4. Deploy with Telegram UI; weekly summary cron.
5. Monitor parsing errors; refine patterns.

**Blockers:**
- Ambiguous input → start with known exercises; add correction command.

**Success:**
- Log in <30s; weekly summary auto; ≥5 logs within 2 weeks.

---

### 3. Dependency Auto-Installer for OpenClaw Skills (GREEN)
**Impact:** High | **Effort:** 9h | **Confidence:** High

**Overview:** Meta-skill that reads `dependencies.json` from each skill and auto-installs missing packages (apt, npm, brew) before registration, making deployment plug-and-play.

**Resource Requirements:** None; builds on existing system.

**Prerequisites:** Skills must include `dependencies.json`; retrofitting existing skills gradually.

**Feasibility:** ✅ Autonomous (requires skill author adoption)

**Implementation:**
1. Define `dependencies.json` schema; create skill structure.
2. Implement resolver: on load, check manifest, run installers, verify.
3. Test with skill missing `jq`.
4. Install as preload hook in gateway config.
5. Update package lists; handle new OS types.

**Blockers:**
- Sudo permissions → configure passwordless for specific commands or manual step.
- Version conflicts → pin versions in manifest.

**Success:**
- New skills auto-install deps; existing broken skills fix with manifest; zero deps failures in 30 days.

---

### 4. HTML Email Calendar Event Creator (GREEN)
**Impact:** Medium | **Effort:** 12h | **Confidence:** High

**Overview:** Monitors boss's HTML emails, parses class schedule tables, auto-creates Google Calendar events. Eliminates manual entry.

**Resource Requirements:**
- Free APIs: Gmail API, Google Calendar API.
- Master must provide Google OAuth client ID/secret and grant access.

**Prerequisites:** Master creates Google Cloud project and shares credentials.

**Feasibility:** ⚠️ Minor Master input needed.

**Implementation:**
1. Set up Google Cloud project; obtain OAuth credentials; store securely.
2. Implement Gmail watch/poll for boss's emails; parse HTML (cheerio); extract fields; create events; handle RRULE.
3. Test with sample email; verify timezone handling.
4. Deploy as cron (every 15 min) or push; log successes/failures to Telegram.
5. Tweak parsing rules as needed.

**Blockers:**
- HTML changes → flexible selectors; allow manual tweaks.
- API quotas → use incremental sync.

**Success:**
- Auto-create events within 5 min of email; zero manual entries for 1 month.

---

### 5. TradingView Alert Auto-Executor for Paper Trading (GREEN)
**Impact:** High | **Effort:** 18h | **Confidence:** High

**Overview:** Receives TradingView webhooks, parses alerts, executes paper trades via Alpaca paper API or local ledger. Includes risk management and Telegram notifications. Safe strategy validation.

**Resource Requirements:**
- Free: Alpaca paper API (or local SQLite).
- Optional: Alpaca paper API keys.

**Prerequisites:** None; Alpaca account free.

**Feasibility:** ✅ Autonomous (Alpaca keys optional).

**Implementation:**
1. Choose backend; create skill structure.
2. Build HTTP endpoint; define alert schema; executor with risk rules; store trades; Telegram alerts.
3. Simulate webhooks; test risk limits and notifications.
4. Deploy endpoint (ngrok/Cloudflare Tunnel); configure TradingView webhook URL; daily P&L cron.
5. Monitor uptime; refine risk params.

**Blockers:**
- Webhook reliability → queue and retry.
- API rate limits → exponential backoff.
- Security → secret token; firewall.

**Success:**
- Alert to trade within 30s; daily P&L summary; complete journal for 30 days.

---

### 6. NFT Liquidity Blueprint & Integration Guide (GREEN)
**Impact:** High | **Effort:** 24h | **Confidence:** High

**Overview:** Comprehensive research guide on built-in NFT liquidity (NFTX, Sudoswap, Seaport). Provides architecture recommendations, example OpenZeppelin contracts, and integration plan for Master's project.

**Resource Requirements:** None; research only.

**Prerequisites:** Knowledge of Master's NFT vision (from ideas).

**Feasibility:** ✅ Autonomous.

**Implementation:**
1. Create GitHub repo `nft-liquidity-blueprint`; outline document.
2. Research protocols; compare trade-offs; draft architecture; write example contracts (mint-with-liquidity, pool, royalties).
3. Compile contracts with Hardhat/Foundry; verify.
4. Publish guide on GitHub Pages; add integration milestones.
5. Monitor ecosystem; update quarterly.

**Blockers:**
- Rapidly changing DeFi → focus on established patterns; recommend formal verification for production.

**Success:**
- Master reads and confirms clear implementation path; can deploy test NFT with liquidity on testnet within 1 week.

---

### 7. Crypto/Web3 Alpha Daily Digest (GREEN)
**Impact:** High | **Effort:** 18h | **Confidence:** High

**Overview:** Scans curated sources (CoinDesk, The Block, Twitter lists via Nitter, Dune Analytics) to produce a daily morning Telegram summary of key insights: token launches, DeFi opportunities, on-chain signals.

**Resource Requirements:**
- Free: RSS feeds, Nitter, Dune free tier.

**Prerequisites:** Define source list and keywords.

**Feasibility:** ✅ Autonomous.

**Implementation:**
1. Curate sources; create skill.
2. Build scrapers (rate limited, dedup); design ranking algo (freshness, credibility, relevance); generate markdown summary.
3. Run test batch; refine ranking; ensure conciseness.
4. Deploy daily cron (7 AM SGT); send to Telegram; add thumbs up/down feedback.
5. Adjust source weights based on feedback.

**Blockers:**
- Rate limits/IP bans → delay requests, rotate user agents.
- Noise → start with high-quality sources; allow Master to ban.

**Success:**
- Digest by 7:15 AM; at least 3 actionable insights/week; feedback improves relevance.

---

### 8. Micro-SaaS Niche Finder & MVP Generator (GREEN)
**Impact:** High | **Effort:** 30h | **Confidence:** High

**Overview:** Identifies underserved micro-SaaS niches with affiliate potential, then auto-generates MVP static site (Next.js) with SEO content and affiliate links. Outputs deployable site and business plan. Rapid passive income testing.

**Resource Requirements:**
- Free: Public SERP data (scraped or free API), Reddit/IndieHackers RSS, ProductHunt API.
- Future hosting: Vercel/Cloudflare free.
- Optional: Affiliate program credentials for live links.

**Prerequisites:** None; MVP generation stops at local build.

**Feasibility:** ✅ Autonomous (build phase).

**Implementation:**
1. Define niche criteria (search volume >100/mo, low competition, affiliate available).
2. Build scouting engine: scrape trends, query SERP, compute opportunity score.
3. For top niches: generate site structure, write SEO articles via StepFun, embed affiliate placeholders, build static export.
4. Store in `micro-saas-projects/`; send Telegram summary.
5. Track which niches pursued to refine scoring.

**Blockers:**
- SERP blocking → use proxies or free SERP API with limits.
- Thin content → high-quality LLM prompts; consider human review before deployment.

**Success:**
- Generate 1-2 MVPs/week; Master reviews >10%; one MVP earns commission within 3 months.

---

### 9. Intelligent Trade Journal & Edge Analyzer (GREEN)
**Impact:** High | **Effort:** 24h | **Confidence:** High

**Overview:** Ingest trades (from TradingView executor or CSV), enrich with market context (BTC dom, SPY, VIX), compute metrics, stratify performance, detect behavioral patterns, produce weekly reports with charts and actionable insights.

**Resource Requirements:**
- Free: Binance/CCXT market data, quickchart.io.
- Optional: data from TradingView Executor skill.

**Prerequisites:** Trade data source (Executor or manual CSV).

**Feasibility:** ✅ Autonomous.

**Implementation:**
1. Design trade schema; create SQLite DB.
2. Implement ingestion (poll Executor DB or CSV upload); fetch market data; compute metrics (win rate, Sharpe, max DD); stratify (by hour, day, volatility, BTC corr); detect patterns (revenge, overtrading).
3. Generate weekly markdown report with quickchart graphs.
4. Deploy weekly cron (Monday 8 AM); Telegram with top insights; add `search` command.
5. Monitor performance; adapt to new regimes.

**Blockers:**
- Missing market data → use benchmarks; allow manual overrides.
- Large histories → incremental processing; cache market data.

**Success:**
- Weekly report by 9 AM; Master gets ≥1 actionable insight/month; complete trade record within 30 days.

---

### 10. Email Autocomplete & Smart Replies (GREEN)
**Impact:** High | **Effort:** 30h | **Confidence:** High

**Overview:** Connects to Gmail, categorizes emails, generates smart reply drafts using StepFun, extracts ToDos. Master reviews and sends drafts, drastically cutting inbox time.

**Resource Requirements:**
- Free: Gmail API, StepFun via OpenRouter.
- Master must provide Google OAuth client ID/secret; grant read/draft access.

**Prerequisites:** Master creates Google Cloud project and shares credentials.

**Feasibility:** ⚠️ Minor Master input needed.

**Implementation:**
1. Obtain OAuth credentials; store securely.
2. Implement Gmail auth/poll; design categories; create prompt templates; integrate StepFun; store drafts in Gmail; extract ToDos; build dashboard/Telegram summary.
3. Test with various emails; evaluate draft quality; adjust prompts.
4. Deploy every 30 min during work hours; Telegram high-priority summary.
5. Implement feedback loop (accept/reject); monitor API usage.

**Blockers:**
- API quotas → batch requests; only process unread.
- Poor drafts → use few-shot examples from Master's past replies.
- Privacy → process locally; no long-term storage of bodies.

**Success:**
- Drafts for ≥80% of non-newsletters; inbox time <15min/day; ToDo accuracy >90%.

---

### 11. Japanese Media Flashcard Automator (GREEN)
**Impact:** Medium | **Effort:** 24h | **Confidence:** High

**Overview:** Takes Japanese media (YouTube), downloads subtitles, extracts sentences with unknown words (based on known list), creates Anki cards with audio snippets and screenshots, pushes via AnkiConnect.

**Resource Requirements:**
- Free: yt-dlp, ffmpeg, local processing.
- Master must run Anki with AnkiConnect addon; provide known-word list.

**Prerequisites:** Anki+AnkiConnect installed and running; known-word list file.

**Feasibility:** ⚠️ Minor Master input (Anki setup).

**Implementation:**
1. Design workflow; create skill.
2. Implement yt-dlp subtitle download (Japanese); tokenize (MeCab); filter unknown; capture screenshot/audio; translate via StepFun; create Anki note via AnkiConnect.
3. Test with sample video; verify cards.
4. Add Telegram command for URLs; summary of added cards; optional daily media scan.
5. Handle failures (missing subs, Anki not running); notify.

**Blockers:**
- No subtitles → fallback to auto-generated; skip if none.
- Tokenization speed → cache known list.
- AnkiConnect down → notify Master to start Anki.

**Success:**
- 10-20 cards per 30min media; cards appear correctly; Master confirms relevance.

---

### 12. Duolingo Progress Analyzer & Custom Practice Generator (GREEN)
**Impact:** Medium | **Effort:** 18h | **Confidence:** High

**Overview:** Syncs with Duolingo to track Japanese progress, identifies weak grammar from mistake patterns (or inferred), fetches real-world sentences from NHK Easy News, generates custom practice quizzes, delivers daily interactive Telegram quizzes.

**Resource Requirements:**
- Free: NHK Easy RSS.
- Master must provide Duolingo credentials (unofficial API).

**Prerequisites:** Duolingo username/password.

**Feasibility:** ⚠️ Minor Master input; risk if API blocked.

**Implementation:**
1. Investigate Duolingo access (unofficial endpoints); store credentials.
2. Implement login; fetch progress/error history (or infer); scrape NHK for matching patterns; generate quizzes via StepFun.
3. Test with sample account; ensure progress sync and quiz quality.
4. Deploy daily practice delivery (e.g., 8 PM); interactive Telegram quiz; track scores.
5. Adjust difficulty based on performance; add Tadoku sources.

**Blockers:**
- API changes/blocking → fallback to headless browser or manual entry.
- Sentence difficulty → filter by JLPT level; use simpler prompts.

**Success:**
- Daily practice delivered; >80% completion; Duolingo accuracy improves 10% in 1 month.

## Quick Win Recommendations
Strictly <4 hours: **None**. However, two ideas can be completed in ~6 hours each and provide immediate value:
- **Skill Health Monitor & Daily Report** (6h) – prevents future skill downtime.
- **Gym Progress Tracker** (6h) – quick win for habit tracking.

These are fully autonomous and require no external setup. Recommend starting with these to build momentum.

## Resource Summary
- **GitHub repos to create:** 1 (`nft-liquidity-blueprint`)
- **New OpenClaw skills/agents:** 11 (all except NFT Blueprint)
- **API keys required:** Google OAuth (Gmail/Calendar), Duolingo credentials, optional Alpaca paper keys
- **Monthly cost estimate:** $0 (all free tiers; no paid services needed)
- **Master action required:**
  1. Create Google Cloud project → provide OAuth client ID/secret for Gmail & Calendar APIs.
  2. Grant API access to the application (one-time).
  3. Install Anki with AnkiConnect addon (for Japanese flashcards).
  4. Provide Duolingo username/password (if enabling that skill).
  5. Optionally create free Alpaca paper account for TradingView executor.

All other tasks can be built autonomously using free, existing resources (OpenClaw skills, free APIs, GitHub Pages, Vercel, etc.).
