# Morning Plan - 2026-03-14

## Executive Summary

- **Total Ideas:** 15
- **Prioritized:** 15 (all feasible with free resources)
- **Quick Wins (<4h):** None (lowest effort 8h). However, 3 ideas have moderate effort (8–15 hours) and can deliver quickly.
- **Estimated Total Effort:** ~405 hours (over 2–3 months part-time)
- **Estimated Monthly Cost:** $0–$1 (micro-fees only)
- **Resources Needed:** ClawHub skills (free), OpenRouter free tier, OpenClaw cron, local storage; Master to provide Google OAuth for email/calendar.

**Top 3 Priorities:**
1. Automated Trading Backtesting Engine
2. Gym Workout Tracker & PR Analytics
3. Email & Calendar Automation Hero

---

## Detailed Plan

### 1. Automated Trading Backtesting Engine (GREEN)

**Impact:** High | **Effort:** 12 hours | **Confidence:** High

**Overview:**  
Build a local backtesting framework by installing and configuring the existing `crypto-backtest` ClawHub skill. It fetches OHLCV data via ccxt and provides EMA/RSI/MACD/Bollinger strategies. We'll add custom strategy definitions and generate performance reports (Sharpe, max drawdown). This de-risks Master's trading bot project with quantitative validation.

**Resource Requirements:**
- 🆓 Free hosting: local machine (no server needed)
- 🆓 Free APIs/models: ccxt exchange public APIs; OpenRouter free tier (optional for LLM strategy suggestions)
- 🧰 OpenClaw Skills needed: `crypto-backtest` (ClawHub)
- 💸 Costs: $0
- 🔑 Master must provide: None (public data)

**Prerequisites:** Install `clawhub` CLI (already available) and run `clawhub install crypto-backtest`.

**Feasibility Assessment:** ✅ Can build autonomously with current resources.

**Implementation Steps:**
1. **Setup (2h):** Install `crypto-backtest` skill; configure exchange credentials (API keys optional for rate limits; public endpoints work without keys); test data fetch for BTC/USDT.
2. **Development (6h):** Define 3–5 sample strategies (e.g., RSI oversold/overbought, MA crossover). Run backtests across 1y of 1h candles. Add custom metrics export (CSV) and simple HTML report.
3. **Testing (2h):** Validate results against known market regimes; ensure no lookahead bias; tweak slippage/fee assumptions.
4. **Deployment (1h):** Add cron job to run weekly backtests and send Telegram summary with key metrics.
5. **Maintenance (ongoing):** Monitor exchange API changes; update data cache monthly.

**Potential Blockers & Mitigations:**
- Exchange rate limits → use aggressive caching (store daily candles locally); add delays.
- Skill limitations → fork skill if needed (allowed).

**Success Criteria:**  
Produce a backtest report for at least one strategy showing Sharpe > 1 over 6 months; automate weekly delivery.

---

### 2. Gym Workout Tracker & PR Analytics (GREEN)

**Impact:** Medium | **Effort:** 8 hours | **Confidence:** High

**Overview:**  
Leverage the existing `gym` skill to log workouts and track personal records. We'll add PR detection (automatic 1RM estimates), weekly summary reports (volume by muscle group), and simple charts (SVG). All data stays local in SQLite. This automates gym progress tracking with zero manual spreadsheet work.

**Resource Requirements:**
- 🆓 Free hosting: local
- 🆓 Free APIs: none
- 🧰 OpenClaw Skills needed: `gym` (ClawHub)
- 💸 Costs: $0
- 🔑 Master must provide: None

**Prerequisites:** `clawhub install gym`.

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (1h):** Install `gym` skill; design SQLite schema (exercises, sets, reps, weight, date, RPE).
2. **Development (4h):** Add CLI/voice interface for quick logging. Implement PR detection algorithm (Epley formula). Create weekly summary generator: total volume, progress flags, upcoming scheduled workouts (from calendar if available).
3. **Testing (2h):** Log sample workouts; verify PR highlights and volume calc.
4. **Deployment (1h):** Configure daily cron to generate weekly summary and send via Telegram every Monday.
5. **Maintenance:** Occasionally adjust muscle-group mappings.

**Potential Blockers & Mitigations:**
- Inconsistent exercise names → implement fuzzy matching and alias list.
- None critical.

**Success Criteria:**  
Master can log a workout in <30 seconds; receives weekly summary with new PRs celebrated.

---

### 3. Email & Calendar Automation Hero (YELLOW)

**Impact:** High | **Effort:** 15 hours | **Confidence:** High

**Overview:**  
Compose `email-daily-summary` (ClawHub) with `gog-calendar` or `gcalcli-calendar` to parse boss's HTML class schedule emails and auto-create Google Calendar events. Handles updates, duplicates, and timezone (SGT). Provides audit log and optional Telegram confirmation.

**Resource Requirements:**
- 🆓 Free hosting: local (cron listener)
- 🆓 APIs: Gmail API, Google Calendar API (free with Google account)
- 🧰 Skills: `email-daily-summary`, `gog-calendar` (or `gcalcli-calendar`)
- 💸 Costs: $0
- 🔑 Master must provide: Google OAuth credentials for email and calendar access (one-time setup)

**Prerequisites:** Master grants OpenClaw access to his Google account via OAuth2 consent (standard flow). We'll provide setup instructions.

**Feasibility Assessment:** ⚠️ Minor Master input needed (Google auth).

**Implementation Steps:**
1. **Setup (3h):** Install skills; guide Master through OAuth flow to obtain refresh token. Test email fetch and calendar access.
2. **Development (8h):** Build parser to identify class schedule emails by sender/subject; extract event details from HTML using heuristics + LLM fallback. Implement create/update/delete logic with idempotency (store message IDs). Add Telegram notification on success/failure.
3. **Testing (2h):** Use sample emails; verify correct event creation; test update and cancellation handling.
4. **Deployment (1h):** Set up cron to run every 15 minutes; ensure resilience (retries, alert on repeated failures).
5. **Maintenance:** Monitor parsing accuracy; refine HTML extraction.

**Potential Blockers & Mitigations:**
- OAuth consent screen → use test user first; Master must approve app in Google Cloud console (we'll guide).
- HTML variability → robust fallback using LLM (OpenRouter free tier) for extraction; cache results to control costs.

**Success Criteria:**  
Boss's class schedule emails result in accurate calendar events within 5 minutes of receipt; no duplicate events; error alerts when parsing fails.

---

### 4. OpenClaw Resource & Performance Telemetry Dashboard (GREEN)

**Impact:** High | **Effort:** 20 hours | **Confidence:** High

**Overview:**  
Extend the existing `openclaw-dashboard` skill with custom metrics: per-agent CPU/memory (via `ps`), token usage (from OpenClaw logs), message throughput, and error rates. Use the `dashboard` skill to visualize metrics in a web UI (localhost). Add alerting (Telegram) on anomalies. Turns OpenClaw into an observable system.

**Resource Requirements:**
- 🆓 Free hosting: local (web server on :3000)
- 🆓 APIs: none; use OpenRouter free LLM for anomaly detection if desired
- 🧰 Skills: `openclaw-dashboard`, `dashboard`
- 💸 Costs: $0
- 🔑 Master must provide: None (local read-only metrics)

**Prerequisites:** `clawhub install openclaw-dashboard dashboard`

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (2h):** Install skills; verify openclaw-dashboard runs and shows basic health.
2. **Development (12h):** Write collector module that polls `openclaw` processes (`ps -o %cpu,%mem,cmd -C openclaw`) every minute, parses gateway/agent logs for token counts and errors, stores in SQLite with timestamps. Create dashboard panels: agent utilization (line chart), token consumption by model (bar), error rate (heatmap). Add optional LLM-based anomaly detection (simple threshold rules first).
3. **Testing (4h):** Simulate load (run heavy tasks); verify metrics capture; test alert triggers.
4. **Deployment (1h):** Run dashboard as background service; add to OpenClaw startup; set up cron for collection.
5. **Maintenance:** Adjust thresholds; extend metrics as OpenClaw evolves.

**Potential Blockers & Mitigations:**
- Log parsing fragility → use regex patterns and fallback to full-text search; version-lock with OpenClaw.
- None show-stopping.

**Success Criteria:**  
Dashboard displays real-time CPU/memory and token usage; Telegram alerts fire on >10% error rate or agent down.

---

### 5. Intelligent Passive Income Opportunity Scout (GREEN)

**Impact:** High | **Effort:** 50 hours | **Confidence:** High

**Overview:**  
Build a research aggregator that scrapes GitHub Issues, Upwork, Fiverr, Reddit, Product Hunt, App Store, and Google Trends. Use LLM to filter noise, score opportunities (demand, competition, alignment), and produce weekly digests with MVP planning checklists. This automates market discovery for passive income ideas.

**Resource Requirements:**
- 🆓 Free hosting: local (cron)
- 🆓 APIs: many public endpoints; Upwork/Fiverr may require account (Master approval to create throwaway accounts for API access) or polite scraping respecting rate limits
- 🧰 Skills: none required; we'll build custom scrapers
- 💸 Costs: $0 (OpenRouter free tier for LLM)
- 🔑 Master must provide: Optional: Upwork/Fiverr credentials if rate-limited; otherwise none.

**Prerequisites:** None.

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (2h):** Define source list; set up SQLite schema for posts and scores.
2. **Development (35h):** Write polite scrapers for each platform (respect robots.txt, add delays). Build NLP pipeline: deduplicate, summarize with LLM (OpenRouter free), categorize (bug, feature request, job, trend). Score algorithm: volume × velocity × alignment. Create weekly digest generator (Telegram message with top 5 ideas + MVP checklist).
3. **Testing (8h):** Run scrapers on test mode; verify no bans; tune LLM prompts for precision.
4. **Deployment (4h):** Schedule cron runs (e.g., nightly); add alert on scraper failure.
5. **Maintenance (ongoing):** Monitor site structure changes; update scrapers quarterly.

**Potential Blockers & Mitigations:**
- IP bans → use exponential backoff, rotate user agents, consider proxy if needed (free Tor? but slow). Keep request rate low.
- LLM cost → limit calls per week (<100); cache results.

**Success Criteria:**  
Weekly digest lists 5–10 viable opportunities; Master confirms at least 1 idea per month gets pursued.

---

### 6. NFT Project Dashboard & Community Monitor (GREEN)

**Impact:** High | **Effort:** 20 hours | **Confidence:** High

**Overview:**  
Compose `nft-tracker` and `dashboard` to create a real-time dashboard for Master's NFT collection: floor price, volume, holders, and social metrics (Twitter mentions, Discord activity via public APIs). Add alerts for floor price drops or spikes.

**Resource Requirements:**
- 🆓 Free hosting: local web UI (dashboard skill) or GitHub Pages
- 🆓 APIs: OpenSea public API (rate-limited, no key needed), Twitter Nitter, Discord public endpoints
- 🧰 Skills: `nft-tracker`, `dashboard`
- 💸 Costs: $0
- 🔑 Master must provide: NFT collection contract address (one-time)

**Prerequisites:** `clawhub install nft-tracker dashboard`

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (2h):** Install skills; configure collection address.
2. **Development (12h):** Build data pipeline: fetch floor/volume from nft-tracker; Twitter mentions via Nitter search; Discord channel member counts (public). Create dashboard panels: metrics over time, social sentiment (LLM). Add alert rules (Telegram).
3. **Testing (4h):** Verify data freshness; simulate price drop to test alerts.
4. **Deployment (1h):** Run dashboard server; optionally push static build to GitHub Pages.
5. **Maintenance:** Monitor API changes; adjust polling intervals.

**Potential Blockers & Mitigations:**
- Rate limits → cache data for 15m; use conditional requests.
- Discord private → only public channels; okay.

**Success Criteria:**  
Dashboard updates within 15m; Telegram alerts trigger on threshold events; Master can view key metrics anytime.

---

### 7. OpenClaw Skill Scaffolding CLI (GREEN)

**Impact:** High | **Effort:** 30 hours | **Confidence:** High

**Overview:**  
Create a CLI tool (`skill-create`) that scaffolds new OpenClaw skills from a template. Generates directory structure, SKILL.md, README, package.json, .gitignore, and optionally creates a GitHub repo with AzureKn1ght as collaborator, sets topics, homepage, and description. Enforces standards and safety defaults.

**Resource Requirements:**
- 🆓 Free hosting: none (CLI tool)
- 🆓 APIs: GitHub API (via `gh` CLI)
- 🧰 Skills: none (pure internal tool)
- 💸 Costs: $0
- 🔑 Master must provide: GitHub personal access token for repo creation (if using GitHub integration)

**Prerequisites:** `gh` CLI installed and authenticated (Master to provide token once).

**Feasibility Assessment:** ⚠️ Minor Master input needed (GitHub token).

**Implementation Steps:**
1. **Setup (2h):** Design template files with placeholders; create `skill-create` binary using commander.js; handle prompts/CLI flags.
2. **Development (20h):** Implement file generation; integrate GitHub repo creation via `gh` or GitHub API (create repo, add collaborator AzureKn1ght, set metadata). Add validation (topic count 5–15). Provide `--no-github` flag for local-only.
3. **Testing (5h):** Create sample skills; verify structure, .gitignore excludes sensitive files; test GitHub integration with test token.
4. **Deployment (2h):** Add to PATH (e.g., `~/.openclaw/bin`); write docs; create example usage video? Not needed.
5. **Maintenance:** Update template as standards evolve.

**Potential Blockers & Mitigations:**
- GitHub rate limits → use authenticated `gh`; token scopes limited to repo creation.
- Template changes → version template; provide upgrade script.

**Success Criteria:**  
`skill-create --name my-skill --description "..." --topics "openclaw,skill,automation"` produces complete skill folder ready for development; optional GitHub repo created with correct settings.

---

### 8. TradingView Signal Aggregator & Alert System (GREEN)

**Impact:** High | **Effort:** 30 hours | **Confidence:** Medium

**Overview:**  
Build a service that polls TradingView chart data (via headless Puppeteer) for configured symbols/indicators, detects signals (e.g., RSI < 30, MA cross), and sends Telegram alerts. Provides config file for symbols, timeframes, and thresholds. This is the first step toward full automation.

**Resource Requirements:**
- 🆓 Free hosting: local (node process)
- 🆓 APIs: none
- 🧰 Skills: none (custom Puppeteer)
- 💸 Costs: $0
- 🔑 Master must provide: None (public TradingView charts)

**Prerequisites:** Install Puppeteer (`npm i puppeteer`). Heavy dependency (~1GB). Use `--no-sandbox` if needed.

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (2h):** Set up Node project; install Puppeteer; test chart screenshot retrieval for BTC/USD.
2. **Development (20h):** Implement chart data extraction: navigate to TradingView widget, wait for data, parse CSV or screenshot OCR? Better: use TradingView's exported data URL? Might be hacky. Alternatively, use ccxt for price and compute indicators locally (simpler). That may be more robust: fetch OHLCV from ccxt and compute indicators directly, avoiding TradingView. This changes approach but still meets goal. We'll adopt ccxt-based indicator engine. Compute RSI, SMA, EMA. Define signal rules in YAML.
3. **Testing (5h):** Test with sample symbols; tune thresholds; ensure no duplicate alerts.
4. **Deployment (2h):** Run as daemon (systemd or OpenClaw agent); send Telegram messages on signal.
5. **Maintenance:** Adjust rules; add new indicators.

**Potential Blockers & Mitigations:**
- TradingView blocking headless → switch to ccxt-based indicator calculation (free and reliable).
- False signals → add confirmation (require 2 consecutive bars).

**Success Criteria:**  
Receives Telegram alert within 1 minute of condition met; <5% false positive rate in backtesting.

---

### 9. Japanese Immersion & Vocabulary Accelerator (GREEN)

**Impact:** High | **Effort:** 30 hours | **Confidence:** High

**Overview:**  
Compose `japanese-translation-and-tutor` with content fetchers (NHK News Easy RSS, YouTube captions, Nitter Japanese tweets). Build daily digest: 5–10 new words with translations, readings, example sentences, and audio links. Add simple spaced repetition review (daily quiz via Telegram). All data local.

**Resource Requirements:**
- 🆓 Free hosting: local
- 🆓 APIs: NHK RSS, YouTube transcript API (free), Nitter instances, Utanet lyrics
- 🧰 Skills: `japanese-translation-and-tutor`
- 💸 Costs: $0
- 🔑 Master must provide: None

**Prerequisites:** `clawhub install japanese-translation-and-tutor`

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (2h):** Install skill; set up SQLite for word database and review schedule.
2. **Development (20h):** Write fetchers for each source; deduplicate; filter by JLPT level (use known vocabulary list). For each new word/phrase, call LLM via OpenRouter free to generate translation and notes. Create daily digest generator (Telegram message with words formatted nicely). Implement SRS algorithm (SM-2) and daily review command.
3. **Testing (5h):** Sample sources; verify translations accurate; ensure no offensive outputs.
4. **Deployment (2h):** Set cron to generate digest each morning; schedule review reminder each evening.
5. **Maintenance:** Add new sources; adjust difficulty.

**Potential Blockers & Mitigations:**
- Copyright issues → use only public RSS/APIs; respect robots.txt.
- LLM hallucinations → validate translations with second LLM call or dictionary API (free Jisho? could use Jisho API). Keep as fallback.

**Success Criteria:**  
Daily digest arrives with 5–10 new words; review compliance >80% after 1 month; Master reports improved vocabulary retention.

---

### 10. Digital Skills Training Intelligence Feed (GREEN)

**Impact:** High | **Effort:** 50 hours | **Confidence:** High

**Overview:**  
Monitor industry sources (LinkedIn Learning, Coursera, Udemy, TechCrunch, Hacker News, conference programs, GitHub trending) to identify emerging digital skills and tools. Use LLM to synthesize trends and compare against Master's expertise; generate weekly digest with course topic suggestions. Supports corporate training business.

**Resource Requirements:**
- 🆓 Free hosting: local
- 🆓 APIs: some platforms have public APIs (GitHub, Hacker News Algolia); others require polite scraping
- 🧰 Skills: none
- 💸 Costs: $0 (OpenRouter free)
- 🔑 Master must provide: None

**Prerequisites:** None.

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (2h):** Define source list; set up SQLite.
2. **Development (35h):** Build scrapers/API clients for each source. Implement deduplication and trend detection (volume velocity). Use LLM to extract skills/tools, compare with Master's known skills (from USER.md), and highlight gaps. Score trends by growth and market size (estimate via job post counts if available).
3. **Testing (8h):** Run on historical data; validate trend detection; refine prompts to reduce noise.
4. **Deployment (4h):** Weekly cron to generate digest and send via Telegram or email.
5. **Maintenance:** Update scrapers when site layouts change.

**Potential Blockers & Mitigations:**
- Legal scraping → obey robots.txt; add delays; use official APIs when available.
- LLM cost → limit calls; batch posts.

**Success Criteria:**  
Weekly digest lists 10–15 trend insights; Master identifies at least 1 new course topic per month.

---

### 11. NFT Liquidity Pool Management & Analytics Dashboard (GREEN)

**Impact:** High | **Effort:** 30 hours | **Confidence:** High

**Overview:**  
Dashboard for NFT liquidity positions (Uniswap V3 NFT, NFTX). Uses NFT APIs plus DeFi protocol subgraphs to track APY, fees, impermanent loss, and rebalancing needs. Composes `nft-tracker` with custom DeFi logic; displays charts and sends alerts.

**Resource Requirements:**
- 🆓 Free hosting: local or GitHub Pages
- 🆓 APIs: Uniswap Subgraph, NFTX API, OpenSea
- 🧰 Skills: `nft-tracker`, `dashboard`
- 💸 Costs: $0
- 🔑 Master must provide: NFT collection contract address (for own collection); wallet address to track positions.

**Prerequisites:** `clawhub install nft-tracker dashboard`

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (2h):** Install skills; note addresses.
2. **Development (20h):** Write DeFi data fetchers: Uniswap V3 NFT positions via subgraph; NFTX pool shares. Calculate metrics: current APY (fees + price change), IL vs HODL, rebalancing triggers. Build dashboard panels with `dashboard` skill.
3. **Testing (5h):** Use public positions (e.g., well-known pools) to validate calculations; compare with expected values.
4. **Deployment (2h):** Run dashboard; set Telegram alerts for fee spikes or IL >10%.
5. **Maintenance:** Monitor protocol upgrades; update subgraph queries.

**Potential Blockers & Mitigations:**
- Subgraph downtime → fallback to direct RPC calls (could be slower); cache aggressively.
- Complex formulas → document assumptions; allow manual overrides.

**Success Criteria:**  
Dashboard shows accurate metrics for at least one active liquidity position; alerts trigger on significant events.

---

### 12. Master's Daily Intelligence Briefing (GREEN)

**Impact:** High | **Effort:** 22 hours | **Confidence:** High

**Overview:**  
Unified daily digest aggregator: upcoming calendar events (next 24h), urgent emails (from boss, flagged), weather (SGT + travel locations), top Web3 pulse (from Web3 Feed), opportunity highlights (from Passive Income Scout), custom watchlist. Uses LLM to condense; delivered via Telegram at 7 AM SGT.

**Resource Requirements:**
- 🆓 Free hosting: local (cron)
- 🆓 APIs: OpenRouter free tier for summarization
- 🧰 Skills: compose `calendar`, `email-daily-summary`, `weather`? (might not exist but we can fetch wttr.in directly), plus outputs from other tools
- 💸 Costs: $0
- 🔑 Master must provide: Google auth for email/calendar; travel location list.

**Prerequisites:** Install relevant skills; configure.

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (2h):** Set up aggregator script with modular sections; define schedule (7 AM SGT).
2. **Development (15h):** Implement each module:
   - Calendar: fetch next 24h events from `calendar` skill.
   - Urgent emails: filter `email-daily-summary` output for boss/flagged.
   - Weather: fetch `wttr.in` for SGT and travel cities.
   - Web3 Pulse: query SQLite from Web3 Trend Feed.
   - Opportunities: query Passive Income Scout DB.
   - Custom watchlist: read from config.
   Use LLM to summarize long texts into 1–2 sentences per item. Format Telegram message with clear headings.
3. **Testing (3h):** Run all modules; verify data freshness; adjust LLM prompts for conciseness.
4. **Deployment (1h):** Add cron (e.g., `0 7 * * *` in SGT) and Telegram send.
5. **Maintenance:** Add/remove modules as new tools are built.

**Potential Blockers & Mitigations:**
- LLM latency → sequential API calls with timeout; fallback to plain text if fails.
- Source failures → include "unavailable" notices and continue.

**Success Criteria:**  
Briefing delivered every morning by 7:15 AM; Master reports it captures all critical info in <2 minutes read.

---

### 13. Autonomous Email Triage & Archival System (YELLOW)

**Impact:** Very High | **Effort:** 30 hours | **Confidence:** High

**Overview:**  
Extend `email-daily-summary` to classify and triage all incoming emails. Use LLM to assign type, urgency, and action required. Auto-archive newsletters older than 30 days, delete spam, flag important senders, move low-priority to "Later" folder. Send daily "Hub" summary of important emails requiring attention with LLM-generated thread summaries.

**Resource Requirements:**
- 🆓 Free hosting: local (cron listener)
- 🆓 APIs: Gmail API (IMAP/OAuth)
- 🧰 Skills: `email-daily-summary` (for baseline access)
- 💸 Costs: $0 (OpenRouter free tier for classification)
- 🔑 Master must provide: Email credentials/OAuth token (same as Email/Calendar task).

**Prerequisites:** Same Google auth as #3.

**Feasibility Assessment:** ⚠️ Minor Master input needed (email access).

**Implementation Steps:**
1. **Setup (2h):** Install `email-daily-summary`; test read/write access to all folders.
2. **Development (20h):** Build classification pipeline: for each new email, call LLM with prompt to output JSON: `{ type: "personal/work/newsletter/promotion/spam", urgency: "high/medium/low", action: "reply/review/ignore", importance: true/false }`. Implement rule engine: auto-archive newsletters >30d, delete spam (confidence >90%), flag important senders (whitelist), move low-priority to "Later". Maintain "hub" label for important threads; generate daily digest at 6 PM.
3. **Testing (5h):** Run on past month of emails (dry run) to evaluate false positives; refine prompts and thresholds.
4. **Deployment (2h):** Set up continuous polling (every 10 minutes) or cron; ensure robust error logging; send daily hub summary via Telegram.
5. **Maintenance:** Periodically review classification accuracy; adjust thresholds.

**Potential Blockers & Mitigations:**
- Misclassifying critical email → start with conservative rules: auto-archive only obvious newsletters; move-to-later instead of delete. Use "importance" flag from LLM as conservative.
- Gmail rate limits → batch operations; use incremental sync.

**Success Criteria:**  
Inbox zero achieved within 1 week of deployment; daily hub summary contains only truly important emails; <1% false positive critical-miss rate.

---

### 14. Web3 Trend Intelligence Feed (GREEN)

**Impact:** High | **Effort:** 40 hours | **Confidence:** High

**Overview:**  
Aggregate Web3 news from Nitter (CryptoTwitter), Reddit r/cryptocurrency, CoinGecko trending, new Dune dashboards, ETHGlobal events, and new token launches. Use LLM to filter noise, summarize, and highlight items relevant to Master's interests (NFT liquidity, DeFi, new tokens). Daily Telegram digest.

**Resource Requirements:**
- 🆓 Free hosting: local
- 🆓 APIs: Nitter (public), Reddit JSON, CoinGecko public, Dune API (may need read-only key), Etherscan public
- 🧰 Skills: none (custom)
- 💸 Costs: $0
- 🔑 Master must provide: Optional Dune API key for higher rate limits; else use public endpoints.

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (2h):** Identify stable Nitter instances; define keywords list.
2. **Development (28h):** Write scrapers for each source with deduplication. Implement LLM summarization: batch posts, extract topic, sentiment, relevance to Master's interests. Score by volume velocity and relevance. Generate daily top 10 digest with links.
3. **Testing (6h):** Validate Nitter stability; tweak LLM prompts to reduce fluff; ensure daily volume manageable.
4. **Deployment (3h):** Cron at 8 AM SGT; Telegram send.
5. **Maintenance:** Switch Nitter instances if blocked; add new sources (e.g., Mirror.xyz).

**Potential Blockers & Mitigations:**
- Nitter downtime → maintain list of fallback instances; auto-switch.
- Rate limits → space out requests; use conditional headers.

**Success Criteria:**  
Daily digest arrives with 5–10 high-signal items; Master identifies at least one actionable insight per week.

---

### 15. Photo Gallery & EXIF Curator (GREEN)

**Impact:** Medium | **Effort:** 18 hours | **Confidence:** High

**Overview:**  
Scan photo directories, extract EXIF (date, GPS, camera), organize by date, generate responsive HTML gallery with thumbnails and lightbox. Optionally deploy to GitHub Pages. Sends Telegram notification when new gallery ready. All local; no external storage.

**Resource Requirements:**
- 🆓 Free hosting: GitHub Pages (optional)
- 🆓 APIs: none
- 🧰 Skills: none; use exiftool and Sharp
- 💸 Costs: $0
- 🔑 Master must provide: Photos directory path (config).

**Prerequisites:** None.

**Feasibility Assessment:** ✅ Can build autonomously.

**Implementation Steps:**
1. **Setup (1h):** Define root photos dir; install exiftool and Sharp.
2. **Development (12h):** Recursively scan, read EXIF, group by YYYY/MM/DD; rename to consistent timestamp; generate WebP thumbnails. Build HTML gallery with responsive grid, lightbox, EXIF popup. Add optional geocoding (via free OpenStreetMap Nominatim) but likely skip due to rate limits.
3. **Testing (3h):** Process sample 100 photos; verify thumbnails; test UI on mobile.
4. **Deployment (1h):** Provide CLI command to build gallery; optional GitHub Pages deploy script (YELLOW: requires git and GitHub repo). Send Telegram with link if hosted.
5. **Maintenance:** Add cron to update gallery weekly.

**Potential Blockers & Mitigations:**
- Large photo collections → process in batches; use streaming.
- EXIF missing → fallback to file modification time.

**Success Criteria:**  
Gallery loads <2s for 200 photos; new photos automatically appear after weekly build; Master can browse from phone.

---

## Quick Win Recommendations

While no idea is under 4 hours, the following three have the highest priority and lowest effort (8–15 hours) and can be delivered within 1–2 weeks part-time:

1. **Automated Trading Backtesting Engine** (12h) – immediate ROI by de-risking trading strategy development.
2. **Gym Workout Tracker & PR Analytics** (8h) – quick morale boost with instant usefulness.
3. **Email & Calendar Automation Hero** (15h) – directly alleviates documented pain point, though requires Google OAuth setup.

We recommend starting with the Backtesting Engine first, as it is fully autonomous and supports Master's high-priority trading bot project.

---

## Resource Summary

**GitHub repos to create:**  
- `openclaw-opportunity-scout` (for Passive Income Scout)  
- `openclaw-web3-feed` (Web3 Trend Feed)  
- `openclaw-training-intel` (Digital Skills Feed)  
- `openclaw-skill-create` (Skill Scaffolding CLI)  
- `openclaw-daily-briefing` (unified aggregator)  
- `openclaw-email-triage` (autonomous triage)  
- Plus any forks of ClawHub skills we customize.

**New OpenClaw skills/agents:**  
Potentially none if we reuse existing skills; our custom tools will be installed via `npm link` or standard OpenClaw agent patterns.

**API keys required from Master:**
- Google OAuth credentials (for Email & Calendar tasks, and Daily Briefing/Email Triage)
- Optional: OpenSea API key (to raise rate limits for NFT dashboards) – can start without
- Optional: Dune API key (for Web3 feed) – can start without

**Monthly cost estimate:** $0. Micro-fees: NFT portfolio tracker charges ~0.001 USDT per call; expected < 30 calls/month = $0.03. No other costs.

**Master action required:**
1. Approve installation of listed ClawHub skills.
2. Provide Google OAuth client ID/secret (or authorize via OpenClaw OAuth flow) for email/calendar access.
3. (Optional) Provide OpenSea/Dune API keys if desired for higher rate limits.
4. Approve creation of listed GitHub repositories (we'll handle setup and publish as needed).

All other implementation can proceed autonomously once prerequisites are met.

---

*End of Plan*
