# Morning Plan - 2026-03-21

## Executive Summary

- **Total ideas:** 12 | **Prioritized:** 12 | **Quick wins:** 3
- **Estimated total effort:** ~70 person-days (can be spread over several weeks)
- **Estimated monthly cost:** $0–$10 (mostly free tiers; potential Pinata overage)
- **Resources needed:** Telegram bot token, OpenRouter API key, Gmail/Google OAuth, Pinata API, Discord bot token, Ethereum testnet RPC, Twitter/Reddit API keys, Duolingo credentials, wallet addresses.

Top 3 priorities:
1. Automated Backup & Integrity Checker
2. Cryptocurrency Portfolio Tracker & Alert System
3. Automated Gym Workout Tracker & Progressive Planner

These three can be deployed within 1–2 days each and provide immediate value with zero cost.

---

## Detailed Plan

### 1. Automated Backup & Integrity Checker (GREEN)
**Impact:** High | **Effort:** 1.5 days | **Confidence:** High

**Overview:** Daily compressed snapshots of critical workspace files with SHA256 checksums. Weekly integrity verification and alerts. One-command restore. Weekly status report. Fully autonomous.

**Resource Requirements**
- 🆓 Hosting: Local cron (gateway)
- 🆓 APIs: None
- 🧰 Skills: Core OpenClaw tools only
- 💸 Cost: $0
- 🔑 Master input: None

**Prerequisites:** Disk space on gateway; write access to workspace.

**Feasibility:** ✅ Build autonomously.

**Implementation**
1. *Setup:* Create config JSON (paths, retention 30d, log path). 2h.
2. *Dev:* Write backup script (tar+gzip, manifest), integrity checker, restore script. 4h.
3. *Test:* Simulate corruption, verify detection, test restore. 2h.
4. *Deploy:* Cron: daily 02:00 SGT, weekly verify Sun 03:00, weekly report Mon 04:00; Telegram alerts on failure. 2h.
5. *Maintenance:* Monitor disk usage; adjust retention if needed.

**Blockers & Mitigations**
- Disk full: implement size check, early alert, auto-purge oldest.
- Slow snapshot: optimize with rsync or split; acceptable at current data size.

**Success Criteria**
- Daily snapshots created.
- Weekly report shows 0 errors.
- Restore to any date within 30d works.

---

### 2. Cryptocurrency Portfolio Tracker & Alert System (GREEN)
**Impact:** High | **Effort:** 2.5 days | **Confidence:** High

**Overview:** Consolidates crypto holdings across wallets via public explorers, computes fiat value using CoinGecko prices, and sends Telegram alerts on >10% portfolio/price moves. Web dashboard shows allocation and P&L charts.

**Resource Requirements**
- 🆓 Hosting: Local + optional Vercel for dashboard (free)
- 🆓 APIs: Etherscan/BscScan free tier, CoinGecko free tier, Telegram Bot API
- 🧰 Skills: Custom OpenClaw skill
- 💸 Cost: $0
- 🔑 Master input: List of wallet addresses; Telegram bot token

**Prerequisites:** None beyond Master providing addresses.

**Feasibility:** ⚠️ Needs Master's wallet addresses and Telegram bot token.

**Implementation**
1. *Setup:* Config JSON with chains/addresses; Telegram token; storage path. 1h.
2. *Dev:* Balance fetchers (EVM, Solana), price fetcher, SQLite storage, alert engine, React dashboard with Chart.js, cron (15min). 10h.
3. *Test:* Sample addresses; simulate price changes; verify alerts/dashboard. 3h.
4. *Deploy:* Deploy dashboard (Vercel or local), set up Telegram webhook/polling, cron. 2h.
5. *Maintenance:* Monitor API rate limits; archive old data; adjust thresholds quarterly.

**Blockers & Mitigations**
- Rate limits: cache prices 5min; backoff.
- New chains: modular fetchers; add as needed.

**Success Criteria**
- Dashboard shows accurate current value.
- Alerts fire within 5 min of threshold breach.
- Data persists across restarts.

---

### 3. Automated Gym Workout Tracker & Progressive Planner (GREEN)
**Impact:** Medium | **Effort:** 3.5 days | **Confidence:** High

**Overview:** Mobile-friendly PWA for quick workout logging. Features: exercise autocomplete, weight/reps entry, voice input, PR auto-detection, progressive overload reminders, progress charts. Data stored in IndexedDB with optional cloud backup via OpenClaw storage. Includes exercise library with demo videos.

**Resource Requirements**
- 🆓 Hosting: Vercel/Cloudflare Pages static hosting (free)
- 🆓 APIs: None (client-side only)
- 🧰 Skills: Vanilla JS/React; optional OpenClaw storage API
- 💸 Cost: $0
- 🔑 Master input: None

**Prerequisites:** None.

**Feasibility:** ✅ Build autonomously.

**Implementation**
1. *Setup:* Project scaffold; IndexedDB schema; chart library; exercise list. 2h.
2. *Dev:* UI components, voice input (Web Speech), PR detection, suggestions, charts, cloud backup, PWA manifest/service worker. 14h.
3. *Test:* Mobile testing, voice accuracy, import/export. 4h.
4. *Deploy:* Deploy to Vercel; add home screen instructions; setup notification reminder. 2h.
5. *Maintenance:* Add exercises; refine suggestions; fix bugs.

**Blockers & Mitigations**
- Voice inaccuracies: fallback manual entry.
- Data loss: encourage cloud backup; export/import.

**Success Criteria**
- Log a workout in <30 seconds.
- PRs automatically recorded and displayed.
- Charts show clear progression over time.

---

### 4. AI-Generated Japanese Web3 Learning Stories (GREEN)
**Impact:** High | **Effort:** 6 days | **Confidence:** High

**Overview:** Daily digest of 2–3 Japanese learning articles sourced from latest Web3/crypto news. LLM rewrites to N4 level with furigana, extracts vocabulary, and creates comprehension questions. Delivered via Telegram each morning. Adjusts difficulty based on Master's feedback.

**Resource Requirements**
- 🆓 Hosting: Gateway cron
- 🆓 APIs: OpenRouter free tier, RSS feeds, Telegram Bot API
- 🧰 Skills: Core OpenClaw tools
- 💸 Cost: $0
- 🔑 Master input: Telegram chat ID; optional feed preferences

**Prerequisites:** None.

**Feasibility:** ✅ Build autonomously.

**Implementation**
1. *Setup:* Select RSS feeds (CoinDesk, The Block, Messari); config for JLPT target; OpenRouter key; DB for tracking. 3h.
2. *Dev:* Fetcher with relevance filter (Japan/Web3 keywords). LLM rewrite pipeline: simplify Japanese, add furigana, extract vocabulary (definitions, readings), generate questions. Assemble digest with markdown formatting. Feedback loop (replies "easy"/"hard"). Cron daily 07:00 SGT. 30h.
3. *Test:* Sample articles; quality check; Telegram formatting test. 5h.
4. *Deploy:* Set up cron, Telegram alerts on failure. 2h.
5. *Maintenance:* Monitor quality; tweak prompts; add feeds every quarter.

**Blockers & Mitigations**
- LLM output quality: iterative prompting; fallback to original if rewrite fails.
- Rate limits: batch articles; free tier sufficient.

**Success Criteria**
- Daily digest arrives by 08:00 SGT.
- Master reports content is comprehensible and relevant.
- Feedback leads to visible difficulty adjustments.

---

### 5. Japanese Learning Content Curator & Spaced Repetition (GREEN)
**Impact:** High | **Effort:** 6 days | **Confidence:** High

**Overview:** Aggregates Japanese content from NHK Easy News, YouTube, podcasts, simplifies to N5–N3, adds furigana and glosses, creates Anki cards via AnkiConnect, and delivers a daily digest. Includes a dashboard to track completion and a custom SRS scheduler.

**Resource Requirements**
- 🆓 Hosting: Gateway cron; optional Vercel for dashboard
- 🆓 APIs: YouTube Data API (free quota), RSS, OpenRouter, AnkiConnect
- 🧰 Skills: Core
- 💸 Cost: $0
- 🔑 Master input: Anki must be running with AnkiConnect enabled; optional YouTube API key

**Prerequisites:** Anki installed and AnkiConnect addon active.

**Feasibility:** ⚠️ Requires Master to set up AnkiConnect.

**Implementation**
1. *Setup:* Define sources (NHK RSS, specific YouTube channels, podcast feeds). Config: difficulty target, Anki deck name. DB for content & reviews. 3h.
2. *Dev:* Build fetchers per source. Simplification pipeline: readability check, LLM add furigana/vocab. Vocabulary extraction (morphological analyzer). Generate Anki cards (note type with fields). Daily digest builder. Dashboard: content library, progress stats, review queue. SRS algorithm (SM-2). 30h.
3. *Test:* Sample content; verify cards created in Anki; digest formatting; dashboard responsiveness. 5h.
4. *Deploy:* Cron daily AM; alert on failures. 2h.
5. *Maintenance:* Add sources; refine simplification; monitor deck growth; adjust SRS parameters.

**Blockers & Mitigations**
- Anki not running: queue cards; notify Master; retry later.
- YouTube transcript missing: use auto-generated; skip if unavailable.

**Success Criteria**
- Daily digest provides 2–3 content pieces + drill items.
- Anki deck expands with relevant cards.
- Weekly review count tracked; retention improves.

---

### 6. Email Triage & Calendar Sync Automation (YELLOW)
**Impact:** High | **Effort:** 4.5 days | **Confidence:** High

**Overview:** Monitors Gmail inbox for class schedule emails from boss. Extracts date/time/topic/location, creates Google Calendar events, archives email, and sends daily digest of processed events and important unread messages.

**Resource Requirements**
- 🆓 Hosting: Gateway cron
- 🆓 APIs: Gmail API, Google Calendar API (free within quotas)
- 🧰 Skills: Custom skill; may reuse email/calendar integrations
- 💸 Cost: $0
- 🔑 Master input: Google Cloud OAuth credentials (client secret) and grant access to account

**Prerequisites:** Master must create a Google Cloud project, enable APIs, and provide credentials.json. This is a manual setup step.

**Feasibility:** ❌ Cannot proceed without Master's OAuth credentials and granting account access. Needs one-time setup.

**Implementation**
1. *Setup:* Guide Master through Google Cloud project creation and OAuth consent; obtain credentials.json. Define email query (from:boss, subject: "class schedule"). Store processed Message-IDs to avoid dupes. 3h (including documentation for Master).
2. *Dev:* Gmail poller (list unread messages matching query). HTML/text parser for date/time/location. Date parsing (dateutil). Create Calendar event with timezone, reminders. Label/archive email. Error handling with alerts. Daily digest generator (previous day's events + unread count). Cron every 15 min during work hours. 22h.
3. *Test:* Use test Gmail or samples from Master; validate parsing; ensure idempotency. 5h.
4. *Deploy:* Cron job; alerts on API failures; documentation. 2h.
5. *Maintenance:* Tweak parser for new email templates; monitor quota usage; add rules as needed.

**Blockers & Mitigations**
- Email format changes: add LLM fallback parser.
- Quota exceeded: batch requests; exponential backoff.
- Token refresh: handle refresh tokens; document renewal.

**Success Criteria**
- All class schedule emails converted to Calendar events within 5 minutes of arrival.
- No duplicate events.
- Daily digest accurate.

---

### 7. Passive Income Opportunity Scanner (GREEN)
**Impact:** High | **Effort:** 7 days | **Confidence:** High

**Overview:** Weekly scanner of freelance platforms (Upwork, Fiverr), digital marketplaces (Gumroad, Teachable), and micro-SaaS forums. Uses LLM to evaluate fit, estimate effort, and draft proposal outlines. Ranks opportunities by (pay × recurrence)/effort and delivers top 5 with next steps.

**Resource Requirements**
- 🆓 Hosting: Gateway cron
- 🆓 APIs: Platform RSS/API where available; web_search; OpenRouter free tier
- 🧰 Skills: Core
- 💸 Cost: $0
- 🔑 Master input: None (but can refine keywords)

**Prerequisites:** None.

**Feasibility:** ✅ Build autonomously using public search and RSS.

**Implementation**
1. *Setup:* Define platforms and keyword list (OpenClaw, AI, automation, training). Config for min budget, max effort. SQLite DB. 2h.
2. *Dev:* Platform fetchers (RSS or site: searches). For each listing: extract title, desc, budget, time. LLM evaluates skill match (1–5), effort (hours), recurrence (1–3). Generate draft proposal outline. Compute ranking score; dedupe. Weekly markdown report (Sunday PM). 35h.
3. *Test:* Run on historical jobs; assess relevance; tweak prompts. 5h.
4. *Deploy:* Cron weekly; Telegram/email delivery; feedback tagging mechanism. 2h.
5. *Maintenance:* Refine keywords; add platforms; monitor LLM usage.

**Blockers & Mitigations**
- ToS/scraping blocks: use official RSS; limit request rate; rotate UA.
- LLM mis-scoring: provide examples; allow manual override in report.

**Success Criteria**
- Weekly report delivered each Monday AM.
- >70% of listings relevant to Master's skills.
- Master pursues at least 1 opportunity per month.

---

### 8. Automated TradingView Signal Aggregator & Alert System (GREEN)
**Impact:** High | **Effort:** 6 days | **Confidence:** High

**Overview:** Continuously scans TradingView charts across assets/timeframes using Puppeteer. Applies configurable technical strategies (MA cross, RSI, support breaks). Generates annotated screenshot and sends Telegram alert with entry/exit, stop-loss, reasoning. Includes paper trading simulation and performance dashboard.

**Resource Requirements**
- 🆓 Hosting: Gateway (with headless Xvfb) or Replit; Vercel for dashboard (free)
- 🆓 APIs: TradingView public widget, Puppeteer (Chromium), Telegram Bot API
- 🧰 Skills: Custom skill; PTY for headless browser
- 💸 Cost: $0 if self-hosted
- 🔑 Master input: List of trading pairs, timeframes, strategy definitions (indicator params)

**Prerequisites:** None.

**Feasibility:** ✅ Build autonomously; note headless browser complexity.

**Implementation**
1. *Setup:* Install Puppeteer dependencies; create strategy config JSON; Telegram token; storage paths. 3h.
2. *Dev:* Chart loader: open TradingView widget (public URL) with symbol/interval. Use page.evaluate to compute signals from indicator values (read from chart). Capture screenshot with overlay shapes. Alert formatter. Paper trading logger (virtual P&L). Dashboard (React + Chart.js) showing signals, equity curve. Cron every 5–15 min market hours. 30h.
3. *Test:* Test signals on sample symbols; verify screenshots; simulate alerts; paper trading accuracy. 5h.
4. *Deploy:* Setup Xvfb if needed; cron; alerts. 2h.
5. *Maintenance:* Tune strategies; add indicators; handle TradingView UI changes; monitor resource usage.

**Blockers & Mitigations**
- TradingView anti-bot: use stealth plugin; delays; respectful intervals.
- Memory leaks: reuse browser context; restart browser daily.
- Indicator drift: rely on TradingView's calculations; avoid custom math.

**Success Criteria**
- Alerts sent within 1 minute of signal.
- Paper trading P&L matches manual backtest.
- Dashboard accurate and updated.

---

### 9. Duolingo Progress Accelerator & Japanese Immersion Dashboard (GREEN)
**Impact:** High | **Effort:** 6 days | **Confidence:** High

**Overview:** Tracks Master's Duolingo Japanese progress via Puppeteer scraping. Identifies mistake patterns (grammar, vocab), generates custom practice sentences, curates real-world Japanese content (NHK, YouTube), and sends daily digest with weak-point drills and immersive material. Includes dashboard with heatmap of skills and mistakes.

**Resource Requirements**
- 🆓 Hosting: Gateway cron; optional Vercel for dashboard
- 🆓 APIs: Puppeteer, OpenRouter, YouTube RSS, NHK RSS
- 🧰 Skills: Core + custom
- 💸 Cost: $0
- 🔑 Master input: Duolingo login credentials (session cookie or username/password)

**Prerequisites:** Master must provide Duolingo credentials to access private progress page.

**Feasibility:** ⚠️ Requires Master's Duolingo credentials. Scraping may break if UI changes.

**Implementation**
1. *Setup:* Secure storage of credentials; identify progress page URLs; define content sources. 3h.
2. *Dev:* Puppeteer scraper: login, fetch skill completion, mistake list per skill. LLM analyze patterns to find top 3–5 weak grammar points. Generate custom sentences with explanations. Fetch NHK Easy News; filter by readability. Get YouTube videos with transcripts. Build daily digest (practice + 1–2 content pieces). Dashboard (React) with skill heatmap, mistake trends, content consumption. 30h.
3. *Test:* Verify scraping accuracy; check pattern analysis; digest formatting. 5h.
4. *Deploy:* Cron daily AM; alert on failure. 2h.
5. *Maintenance:* Update selectors if Duolingo changes; refine weak-point detection; add new content sources.

**Blockers & Mitigations**
- 2FA: ask Master to provide session cookie to bypass.
- Rate limits: scrape once daily only.
- Content duplication: track seen articles.

**Success Criteria**
- Digest arrives each morning.
- Practice sentences target actual weak areas.
- Dashboard reflects true progress.

---

### 10. Crypto Narrative & Social Sentiment Radar (GREEN)
**Impact:** High | **Effort:** 9 days | **Confidence:** High

**Overview:** Monitors Twitter, Reddit, Telegram to cluster posts into crypto narratives. Tracks social volume and sentiment; correlates with on-chain metrics (active addresses, tx volume). Alerts when a narrative related to Master's holdings gains momentum. Daily report and web dashboard.

**Resource Requirements**
- 🆓 Hosting: Gateway daemons; Vercel for dashboard
- 🆓 APIs: Twitter API (free tier), Reddit API, Etherscan/BscScan, DeFiLlama, OpenRouter embeddings
- 🧰 Skills: Custom
- 💸 Cost: $0
- 🔑 Master input: Twitter API keys (consumer/secret), Reddit client ID/secret; list of wallet holdings for alert correlation

**Prerequisites:** Master must create Twitter developer app and Reddit app to get API credentials.

**Feasibility:** ⚠️ Requires Master's Twitter/Reddit API keys.

**Implementation**
1. *Setup:* Register Twitter app and Reddit app; store keys. Define crypto keyword list; wallet holdings list. SQLite schema. 4h.
2. *Dev:* Collectors: Twitter stream/polling; Reddit submissions/comments; optional Telegram. For each post, compute embedding (OpenRouter text embedding). Cluster: either use online clustering (DBSCAN on recent vectors) or LLM categorization with manual labeling interface. For each cluster: volume timeline, sentiment (small model or LLM). Fetch on-chain metrics for associated tokens (DeFiLlama). Correlation logic: if both social volume and on-chain activity spike (>50%) for cluster containing Master's assets, alert. Daily report generation: top narratives, stats, influential posts. Dashboard: line charts, bubble charts. 45h.
3. *Test:* Run on historical data; validate clustering quality; tune thresholds. 7h.
4. *Deploy:* Daemons (or cron 30min) for collectors; dashboard on Vercel; Telegram alerts. 4h.
5. *Maintenance:* Add sources; refine clustering; add new metrics; monitor rate limits.

**Blockers & Mitigations**
- Rate limits: use sampling; store raw data only short-term.
- Clustering noise: add manual labeling UI to correct misclusters; improve embeddings.
- False alerts: require both social and on-chain spikes.

**Success Criteria**
- Daily report generated without errors.
- Alerts fire on genuine narrative surges.
- Dashboard updates in near real-time (<10 min lag).

---

### 11. NFT Project Launch Readiness Assistant (YELLOW)
**Impact:** High | **Effort:** 12 days | **Confidence:** High

**Overview:** CLI/web dashboard to guide NFT collection launch. Validates smart contracts (Slither), metadata (ERC-721/1155), uploads assets/metadata to IPFS via Pinata, generates allowlist (Merkle tree) scripts, creates Discord verification bot, and provides a countdown with marketing checklist. All steps tracked with manual approval gates before any on-chain action.

**Resource Requirements**
- 🆓 Hosting: Local; Discord bot can run on free Replit or gateway
- 🆓 APIs: Slither (local), Pinata free tier (1GB), Discord Bot token, Ethereum testnet RPC (Infura free)
- 🧰 Skills: Custom; install Slither and Discord.js
- 💸 Cost: $0 (Pinata overage may require $10 if >1GB)
- 🔑 Master input: Pinata API key, Discord bot token, Infura/Alchemy project ID, wallet private key (for testnet)

**Prerequisites:** Master must provide API keys and a wallet for testnet deployment.

**Feasibility:** ⚠️ Minor Master input needed (API keys, wallet). Complex integration.

**Implementation**
1. *Setup:* Install Slither, Node dependencies (ipfs-http-client, merkle-tree, discord.js). Create project scaffold with config (collection name, supply, royalty%). Prepare API keys from Master. 4h.
2. *Dev:* Validation modules: (a) Slither run; parse output; show warnings/errors. (b) Metadata JSON schema check; image format/size validation. IPFS upload: batch images and metadata to Pinata, record CIDs. Allowlist generator: read CSV of addresses, build Merkle tree, generate proof script. Discord bot: role management, mint alerts, verification command. Interactive checklist UI (TUI or web) with step status and logs; manual approval gates before any network call. Countdown timer. Optional: social media scheduler. 60h.
3. *Test:* Deploy contract to Sepolia testnet; run full validation; IPFS upload; generate allowlist; test Discord commands; simulate launch flow. 8h.
4. *Deploy:* Package as skill: commands `nft-launch init`, `validate`, `upload`, `allowlist`, `launch`. Documentation. Add cron for countdown reminders. 4h.
5. *Maintenance:* Update dependencies; add support for new standards; monitor Pinata usage.

**Blockers & Mitigations**
- Contract vulnerabilities: Slither may flag; manual review step.
- IPFS pin limits: notify if approaching 1GB; suggest Pinata paid plan.
- Discord bot rate limits: enable intents; consider sharding if many servers.

**Success Criteria**
- All validation steps pass for test project.
- IPFS upload succeeds without errors.
- Allowlist proof script works.
- Discord bot responds to commands.
- Launch checklist reaches 100% with no unresolved failures.

---

### 12. Automated Corporate Training Slide Deck & Material Generator (GREEN)
**Impact:** High | **Effort:** 6 days | **Confidence:** High

**Overview:** Generates professional slide decks and training materials from topic, audience, and duration. Researches latest data via web search, creates outline with learning objectives, generates slide content and speaker notes using LLM, and exports to PowerPoint (pptx) or Google Slides. Includes interactive quiz slides and handouts.

**Resource Requirements**
- 🆓 Hosting: Local or Vercel for web UI
- 🆓 APIs: web_search, OpenRouter free, pptxgenjs (local), optional Google Slides API (requires OAuth)
- 🧰 Skills: Custom; may reuse paragraph skill
- 💸 Cost: $0
- 🔑 Master input: None (Google OAuth optional if using Slides)

**Prerequisites:** None; Google OAuth needed for Slides export.

**Feasibility:** ✅ Build autonomously with PPTX export; Slides optional.

**Implementation**
1. *Setup:* Define input schema (topic, audience, duration, objectives). Design outline template and slide theme colors/fonts. Storage for decks. 2h.
2. *Dev:* Research module: web_search top 5 recent articles; fetch and summarize key points. LLM outline generator: learning objectives, section breakdown, time allocations. Slide content generator: per slide: title, bullet points (≤5), speaker notes (detailed), suggested visuals. PPTX export using pptxgenjs: create slides, apply theme, insert images (placeholders). Handouts: cheat sheet (format with key terms), quiz questions (mult choice, true/false) with answers. Optional Google Slides integration (OAuth flow). Review mode: browser preview with edit capabilities. Multiple design templates. 30h.
3. *Test:* Generate deck for sample topic; evaluate slide quality, layout; test speaker notes; quiz generation. 5h.
4. *Deploy:* Package as OpenClaw skill with CLI `gen-deck`. Add documentation; optional web UI. 2h.
5. *Maintenance:* Update research queries; improve outline logic; add templates; incorporate user feedback.

**Blockers & Mitigations**
- Web search rate limits: cache results; use few keywords.
- LLM hallucinations: verify with multiple sources; add disclaimer to review facts.
- Layout issues: thorough testing; fallback simple theme.

**Success Criteria**
- Generated decks are professional and factually accurate.
- Deck size matches intended duration (≈1–2 slides per minute).
- Includes quiz and handouts.
- Reduces Master's prep time by >50%.

---

## Quick Win Recommendations

The highest-priority ideas that can be fully deployed within 1–2 days with zero cost and minimal setup:

1. **Automated Backup & Integrity Checker** – Protects all work immediately; no input needed.
2. **Cryptocurrency Portfolio Tracker & Alert System** – Requires just wallet addresses and a Telegram bot; provides real-time portfolio visibility.
3. **Automated Gym Workout Tracker & Progressive Planner** – No setup; instant mobile app for fitness; improves wellbeing.

These three can be started right away after approval and will deliver ongoing value with little maintenance.

---

## Resource Summary

- **GitHub repos to create:** One per idea (12 total) or a single monorepo with 12 modules. Significant codebase (~2000 LOC total).
- **New OpenClaw skills/agents:** 12 new skills (one per idea) consolidated under a single "planner" skill with subcommands or separate skills.
- **API keys required:**
  - Telegram Bot token (one for many)
  - OpenRouter API key (free tier)
  - Gmail/Google Calendar OAuth (for Email Triage)
  - Pinata API key (NFT Launch)
  - Discord bot token (NFT Launch)
  - Ethereum testnet RPC provider (Infura/Alchemy) (NFT Launch)
  - Twitter API keys (Crypto Radar)
  - Reddit app credentials (Crypto Radar)
  - YouTube Data API key (optional, JP Curator)
  - Duolingo credentials (JP Dashboard)
  - Wallet addresses (Portfolio Tracker)
- **Monthly cost estimate:** $0 if using free tiers exclusively; potential $10 if Pinata storage exceeds 1GB (unlikely initially). No recurring API costs within free limits.
- **Master action required:**
  1. Provide Telegram chat ID (most projects).
  2. Provide wallet addresses for Portfolio Tracker.
  3. Create Google Cloud OAuth credentials for Email Triage.
  4. Create Pinata account and API key for NFT Launch.
  5. Create Discord bot and provide token for NFT Launch.
  6. Obtain Infura/Alchemy project ID for NFT Launch testnet.
  7. Create Twitter developer app and Reddit app for Crypto Radar.
  8. Provide Duolingo login for JP Dashboard.
  9. Approve deployment of GREEN tasks and provide YELLOW prerequisites.

All GREEN tasks can begin immediately after approval; YELLOW tasks will require the corresponding Master-provided credentials before proceeding.

---

*Plan generated by Strategic Morning Planner for 2026-03-21*
