---
title: "Daily OpenClaw Configuration Backup - Failed (SIGTERM Timeout)"
date: 2026-03-08
task_id: 7458e079-8b91-4aa8-a864-9639291eb173
agent: main
status: failed
score: 2
---

The daily backup cron started at 22:00 UTC. The agent executed `/home/node/.openclaw/workspace/bin/backup-to-gdrive.sh` and monitored it via process polling. The script made steady progress, uploading many files and directories (skills, memory, AARs, agent sessions) to Google Drive folder "OpenClaw Backups/2026-03-08". Approximately 10 minutes into execution (at 22:10:07), the process received a SIGTERM signal (likely due to the 600-second timeout set by the agent). The backup was terminated before completion; the script did not reach housekeeping (deduplication, cleanup) and did not send the expected success summary (which the script itself handles). The agent correctly identified the failure and sent a Telegram alert to notify Master. Root cause: timeout too short for the amount of data; the full backup takes >10 minutes. The agent's monitoring was appropriate, but the timeout should be increased or the backup should run detached without a hard timeout. Recommend either (1) increase timeout to 1800 seconds and ensure monitoring continues until natural completion, or (2) run the backup script via cron directly without agent supervision, letting the script handle its own notifications. The backup is now incomplete and must be re-run.
