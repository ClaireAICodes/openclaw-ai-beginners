### [GREEN] Playwright Dependency Preflight & Self-Healing Wrapper
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A reusable Node.js module that Playwright-based skills invoke before launching the browser. It checks for required system libraries (libnspr4, libatk1.0-0, libatk-bridge2.0-0, libx11-xcb1, etc.), attempts automatic apt-get installation if missing, and verifies installation before proceeding. Wraps any Playwright script to ensure dependencies are met.

**Rationale:**
Today's MS Forms submission failure due to missing libnspr4 is symptomatic of a broader risk: any Playwright skill can break when system libraries are absent, especially on fresh deployments or after system updates. A preflight check prevents wasted cron runs, reduces debugging time, and enables self-healing for all automation relying on browser interaction.

**Implementation Tasks:**
- Catalog required system libraries for Playwright Chromium on Debian/Ubuntu (from official Playwright docs)
- Build a lightweight Node script that checks library presence via `ldconfig -p` and `which` for playwright itself
- Implement auto-install via `sudo apt-get update && sudo apt-get install -y <missing-packages>` with special handling for passwordless sudo in OpenClaw context
- Add retry logic: after installation, re-check before failing
- Create a wrapper function that skills can `require('./preflight')` and call `ensureDependencies()` before launching browser
- Update ms-forms-auto scripts to use the wrapper
- Test by uninstalling libnspr4 and verifying auto-recovery

---
### [GREEN] Consolidated OpenClaw Health Command
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A single CLI command (`openclaw health`) that aggregates the health status of the entire OpenClaw ecosystem: cron job execution history (recent runs, exit codes), installed skills (versions, outdated), system dependencies (missing packages), backup system (last success), and external integrations (auth validity, API connectivity). Outputs a concise health score (0-100) and a list of urgent alerts with fix commands.

**Rationale:**
Currently, system health is scattered across logs, memory files, and manual checks. Master lacks a quick health overview, leading to delayed detection of issues (like today's backup and MS Forms failures). A consolidated health command provides at-a-glance status, reduces cognitive load, and speeds up troubleshooting by surfacing actionable alerts in one place.

**Implementation Tasks:**
- Define health metrics: cron success rate (last 5 runs), skill uptime (last execution), dependency completeness (installed vs required), backup integrity (last run, warnings), auth validity (expiry dates)
- Build a Node.js script that queries each component: read cron logs, check daily-entries, run dependency checks, invoke backup dry-run, verify storageState files
- Create a scoring algorithm: assign weights and compute overall health percentage
- Generate actionable alerts: e.g., "Missing: libnspr4 → Run: sudo apt-get install -y libnspr4"
- Integrate as an OpenClaw plugin (`openclaw health`) or standalone script in bin/
- Add optional Telegram summary output for proactive monitoring
- Test on current system to verify accurate health state and useful recommendations

---
### [GREEN] Automated Trading Signal Aggregator & Backtester
**Category:** Trading & Investment
**Risk Level:** green
**Confidence:** High

**Description:**
A Node.js service that aggregates trading signals from multiple curated sources (Twitter influencers, TradingView ideas, crypto news RSS) and stores them in a structured database. It includes a backtesting module that simulates signal performance on historical data to identify high-probability patterns. Outputs a daily ranked list of signals with confidence scores.

**Rationale:**
Master is actively interested in trading and seeks an edge. Manually tracking signals is time-consuming and inconsistent. An automated aggregator with backtesting provides objective validation, filtering noise, and surfacing alpha that would be missed otherwise. This directly supports his goal of developing a trading bot with market edge.

**Implementation Tasks:**
- Identify and catalog reliable signal sources (Twitter API, TradingView public scripts, crypto news feeds)
- Build a scraper/collector module with rate limiting and deduplication
- Design a schema for storing signals (source, asset, direction, timeframe, confidence, predicted outcome)
- Implement backtesting engine using historical price data (from Binance, CoinGecko, or Yahoo Finance)
- Create daily report generator (Markdown/Telegram) with top signals and performance stats
- Set up cron job to run daily and output to Research/signals/ directory

---
### [GREEN] Personalized Japanese Learning Accelerator
**Category:** Learning & Skills
**Risk Level:** green
**Confidence:** High

**Description:**
A personalized Japanese learning pipeline that delivers daily curated content: vocabulary (with example sentences), grammar points, and cultural notes. It pulls from open resources (Tae Kim's guide, Jisho.org, NHK News Web Easy) and adapts to Master's Duolingo progress (if accessible). Includes daily quiz via Telegram to reinforce retention.

**Rationale:**
Master is learning Japanese via Duolingo but wants to accelerate and complement with richer context. Custom-tailored content delivered daily fits his busy schedule and makes consistent progress effortless. By automating curation and delivery, we remove the friction of searching for quality material, keeping motivation high.

**Implementation Tasks:**
- Research and select source APIs/feeds for Japanese learning content (Jisho API, Satori Reader, NHK)
- Build a content fetcher that selects appropriate difficulty level based on Duolingo skill tree (scraping if no API)
- Create a template for daily lesson (5-10 min) with 3-5 vocabulary, 1 grammar point, 1 cultural note
- Integrate with Telegram to send daily lesson and follow-up quiz the next day
- Implement simple tracking (SQLite) to avoid repeats and monitor progress
- Add periodic review sessions using spaced repetition logic
---
### [GREEN] Intelligent Email Triage & Auto-Responder
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
An OpenClaw skill that uses gog (Gmail API) to fetch new emails, classify them using simple rules and AI, and take automated actions: label, send canned responses, and surface urgent messages via Telegram. Low-priority emails are deferred; the system learns from feedback to improve classification.

**Rationale:**
Master wastes mental energy manually sorting emails and missing important ones in the noise. Automating triage ensures he sees only what matters, responds promptly, and achieves inbox zero with minimal effort.

**Implementation Tasks:**
- Set up gog Gmail read/write scopes (already available via existing credentials)
- Design a labeling taxonomy (Urgent, Important, Low, Newsletters)
- Implement rule engine: keyword/sender-based filters plus LLM-based importance scoring
- Create actions: apply labels, send auto-responders, Telegram alerts for urgent, daily digest for low
- Build feedback loop: Master can reply with /important or /junk to retrain classifier
- Schedule cron job every 30 minutes during work hours

---
### [GREEN] Calendar Invite Parser from HTML Emails
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A skill that monitors Gmail for HTML emails from the boss containing class schedules. It parses event details (date, time, course name, location) and automatically creates Google Calendar events via gog, with conflict detection and reminders.

**Rationale:**
Boss sends class info via HTML email without calendar invites, forcing manual entry which is error-prone and time-consuming. Automating extraction and creation ensures accurate scheduling and saves time.

**Implementation Tasks:**
- Configure gog Gmail search filter: from boss@domain.com with subjects like "Class Schedule"
- Build HTML parser to extract event details using regex and date parsing libraries
- Normalize extracted dates/times to RFC3339 format
- Use gog calendar create command to add events with default reminders
- Implement de-duplication: check for existing events with matching course/time/location
- Send Telegram confirmation with event details and link to calendar; allow deletion with /cancel
---
### [GREEN] OpenClaw Skill Scaffolding Wizard
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A CLI wizard (`openclaw skill create`) that scaffolds a new skill project with the optimal structure: standard directories (src, tests, docs), pre-configured linting and testing, GitHub Actions CI for Node.js, and auto-generated README and skill.json metadata based on user input (name, description, category). It also automates post-creation steps: add as git remote, push, add AzureKn1ght as collaborator, tag with standard topics, and install the skill locally.

**Rationale:**
Creating a new skill currently involves manual setup that is error-prone and inconsistent. A standardized scaffolding enforces best practices, accelerates development, and ensures every skill is ready for CI/CD and collaboration from day one.

**Implementation Tasks:**
- Design the canonical skill project layout (directories, config files, templates)
- Build an interactive CLI using prompts for skill name, description, category, and initial commands to support
- Generate files from templates: package.json, README.md, skill.json, GitHub Actions workflow, lint config, test stub
- Implement post-creation automation: git init, add remote, push, collaborator API call, topic tagging, local symlink installation
- Add self-update capability to pull latest template changes
- Test end-to-end by creating a sample skill and verifying full pipeline

---
### [GREEN] NFT Liquidity Pool Monitor & Advisor
**Category:** Web3 & Crypto
**Risk Level:** green
**Confidence:** High

**Description:**
A monitoring service that tracks your NFT collection's floor price, pool liquidity, and trading volume on marketplaces (OpenSea, LooksRare) via their APIs or The Graph. It analyzes trends and sends Telegram alerts with actionable recommendations: when liquidity is low (suggest adding), when price is volatile (suggest adjusting), or when arbitrage opportunities appear. Includes a simple dashboard (Markdown report) and historical data logging.

**Rationale:**
Master's NFT project requires built-in liquidity to succeed, but manual monitoring is tedious and slow. Automated, timely insights enable quick decisions to stabilize floor price and maximize revenue, making the collection more attractive to buyers.

**Implementation Tasks:**
- Identify and access NFT collection data sources (OpenSea API, LooksRare subgraphs)
- Set up a scheduler (cron) to fetch data every 15 minutes during active hours
- Compute key metrics: floor price, total liquidity, volume last 24h, bid/ask spread
- Define alert rules (e.g., liquidity < threshold, floor price drop >5%)
- Build Telegram integration to send alerts with recommended actions and links
- Generate daily summary reports stored in Research/nft/ with charts (ASCII or markdown)
- Log data to JSONL for future analysis

---
### [GREEN] Cryptocurrency Portfolio Tracker with Smart Alerts
**Category:** Trading & Investment
**Risk Level:** green
**Confidence:** High

**Description:**
A Node.js service that connects to crypto exchange APIs (Binance, Coinbase, etc.) to fetch current holdings and prices, calculates total portfolio value, and sends Telegram alerts when total drops or rises by configurable thresholds (e.g., -10% or +15%). Also tracks daily P&L and weekly trends.

**Rationale:**
Master is actively trading and holds crypto assets. Manually checking portfolio across multiple exchanges is tedious and easy to neglect. Automated tracking with threshold alerts provides timely awareness of significant movements and helps in making informed decisions without constant monitoring.

**Implementation Tasks:**
- Identify exchange APIs and obtain API keys (with read-only permissions)
- Design unified portfolio schema (exchange, asset, quantity, value)
- Build fetcher module that queries each exchange and aggregates totals in local SQLite
- Implement threshold checking and Telegram alert logic
- Add daily digest at a set time with portfolio snapshot and 24h change
- Secure storage for API keys using OpenClaw config secrets

---
### [GREEN] Passive Income Idea Validator
**Category:** Passive Income & Businesses
**Risk Level:** green
**Confidence:** High

**Description:**
A research engine that scrapes data from Reddit (r/indiehackers, r/passive_income), digital product marketplaces (Gumroad, Udemy), and freelance platforms (Upwork, Fiverr) to discover trending side-hustle ideas. It scores each idea on competition (search volume, product count), demand (subscriber growth, keyword trends), and monetization potential (average price, commission rates). Outputs a ranked list of vetted opportunities weekly.

**Rationale:**
Master seeks passive income streams but lacks bandwidth to manually research trends and competition. This automated validator distills raw data into actionable shortlists, highlighting high-potential, low-competition niches he can target with minimal risk.

**Implementation Tasks:**
- Identify and catalog target data sources and their APIs or scraping methods
- Build scrapers with rate limiting and error handling for each source
- Design scoring algorithm weighted by competition, demand, and monetization metrics
- Store results in markdown reports under Research/passive-income/
- Set up weekly cron job (Sunday 8 AM) to generate and send Telegram summary
- Include direct links to top opportunities for quick follow-up
---
### [GREEN] NFT Liquidity Dashboard
**Category:** Web3 & Crypto
**Risk Level:** green
**Confidence:** High

**Description:**
A monitoring dashboard for NFT collections that tracks liquidity metrics across major marketplaces (OpenSea, LooksRare, X2Y2). It records floor prices, trading volume, number of listings, and bid/ask spreads, and sends daily summaries with alerts for unusual drops or spikes. Data stored in SQLite; optional Telegram alerts.

**Rationale:**
Master's NFT project ambitions require understanding market liquidity to price effectively and decide on built-in liquidity features. A dedicated dashboard automates the tedious manual tracking and provides actionable insights for optimizing listings and detecting market movements early.

**Implementation Tasks:**
- Identify public API endpoints for OpenSea (v2), LooksRare, X2Y2 for collection stats and listings
- Design a SQLite schema: collections, daily_stats, listings
- Build a fetcher that queries APIs with rate limiting and caching
- Implement metrics calculation: floor price, average price, spread, liquidity depth
- Generate daily Telegram report with graphs (using quickchart or ascii charts)
- Add alerts: if floor price drops >5% in 24h or volume spikes >200%

---
### [GREEN] Affiliate Income Tracker
**Category:** Passive Income & Businesses
**Risk Level:** green
**Confidence:** High

**Description:**
A service that aggregates earnings from multiple affiliate programs (Amazon Associates, ClickBank, ShareASale, etc.) into a unified dashboard. It fetches daily commission data via APIs or CSV exports, normalizes by product and category, and produces a daily earnings report with trends and predictions. Also detects missing or delayed payouts.

**Rationale:**
Master wants passive income streams. Affiliate marketing is a viable channel, but tracking multiple dashboards manually is time-consuming and obscures the bigger picture. An automated aggregator gives a clear view of performance, identifies top offers, and flags issues (like zero commissions) promptly.

**Implementation Tasks:**
- Catalog affiliate APIs/export methods for popular networks (Amazon Product Advertising API, ClickBank API, ShareASale feed)
- Create secure config for storing credentials and session tokens (gitignored)
- Build fetchers for each network, handling authentication and error retries
- Normalize data into a common schema: date, program, product, category, commission, status
- Store in SQLite; generate daily summary with top categories, month-to-date earnings
- Send Telegram digest each evening; provide /earnings command for on-demand query
---
### [GREEN] OpenAI API Cost Optimizer & Usage Dashboard
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A service that tracks OpenAI (and other LLM provider) token usage across all OpenClaw skills, calculates costs, and provides insights on savings opportunities: identify high-frequency cheap models, cache frequent prompts, suggest model downgrades where quality impact minimal, and set usage alerts to avoid budget overruns.

**Rationale:**
Master uses AI extensively for automation and ideation. Without visibility, costs can balloon unexpectedly. Automated monitoring and optimization ensures he gets the most value per dollar and prevents bill shock.

**Implementation Tasks:**
- Instrument all LLM calls to log prompt/response tokens, model, skill name, and timestamp to a central database
- Build aggregation queries for daily usage and cost calculation based on provider pricing
- Create a dashboard (CLI or Telegram) showing top-consuming skills, estimated monthly spend, and trending
- Implement suggestions engine: detect repeated prompts >5 times and enable caching, alert when model choice is overkill for task type
- Set budget alerts (e.g., 80% of monthly budget) with Telegram notifications
- Optionally apply simple optimizations automatically (temperature tuning, context pruning)
- Secure API key management using OpenClaw config (gitignored)
---
### [GREEN] Gym Workout Auto-Logger & Strength Progress Tracker
**Category:** Lifestyle & Habits
**Risk Level:** green
**Confidence:** High

**Description:**
A skill that automates gym workout logging through voice commands or a quick Telegram form. Captures exercise, sets, reps, weight. Stores in SQLite. Generates weekly progress reports with 1RM estimates and trends. Integrates with calendar to remind of workout days.

**Rationale:**
Master goes to gym weekly but manually tracking is tedious and often forgotten. Automating logging ensures consistent records, enabling progress tracking and more informed training adjustments without mental overhead.

**Implementation Tasks:**
- Design simple Telegram bot interface: send /log to start workout, then stepwise prompts for exercise, sets, reps, weight
- Alternatively, accept voice note transcription via LLM to extract data
- Store data in SQLite with schema: date, exercise, sets, reps, weight, notes
- Compute 1RM (one-rep max) estimates using standard formulas (Epley, Brzycki)
- Generate weekly summary (Markdown) with total volume, top exercises, progress vs previous week
- Set up cron to send reminder on scheduled gym days (from calendar or preset)
- Provide query command /stats to show latest stats on demand

---
### [GREEN] Corporate Training Content Generator
**Category:** Work & Career
**Risk Level:** green
**Confidence:** High

**Description:**
A tool that helps Master quickly create training materials from a topic outline. Uses LLM to generate course slides (markdown), speaker notes, quiz questions, and practical exercises. Outputs structured files in a standardized course template, ready for review and delivery.

**Rationale:**
As a corporate trainer, Master frequently develops new courses. The initial content drafting is time-intensive. This tool automates the first draft, giving him a solid starting point that he can refine, thereby accelerating course creation and scaling his output.

**Implementation Tasks:**
- Create a prompt template that takes course topic, learning objectives, and duration
- Use LLM (via gpt-4 or claude) to generate: 1) slide deck (markdown with titles, bullet points, speaker notes), 2) quiz questions (multiple choice with answers), 3) hands-on exercise descriptions
- Store outputs in a course-specific directory (Courses/<topic>/) with organized file structure
- Add post-generation quality checks: run generated code samples through a sandbox, verify quiz answer validity
- Integrate with Telegram to receive a zip of the generated course for review
- Allow feedback loop: Master can reply with edits, which are incorporated for regeneration

---
### [GREEN] Multi-Agent Orchestrator & Skill Registry
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A centralized registry and orchestration layer for OpenClaw agents and skills. It maintains a catalog of all available agents (sub-agents, ACP sessions), their capabilities, current status (idle/busy/error), and performance metrics. The orchestrator routes incoming tasks to the most appropriate agent based on specialization, load, and historical success rates. Includes a CLI (`openclaw agents`) and Telegram commands for real-time oversight and manual dispatch.

**Rationale:**
As the OpenClaw ecosystem grows, manual agent management becomes unwieldy. Without visibility into which agent can do what and their current state, task routing is inefficient and errors go unnoticed. An orchestrator ensures optimal utilization, prevents overloading, and provides Master with a single pane of glass to monitor and control the entire agent fleet—critical for scaling automation reliably.

**Implementation Tasks:**
- Define agent metadata schema: id, name, description, supported commands/tools, tags, max_concurrent_tasks
- Build registry storage (JSON file or SQLite) with CRUD operations for agent registration/deregistration
- Implement heartbeat mechanism: agents periodically report status and metrics (last task, success/failure count, avg duration)
- Create routing engine: given a task description, match against tags and capabilities to select best available agent (considering load and recent failures)
- Build CLI and Telegram interface: `/agents list`, `/agents status <id>`, `/agents dispatch <task> [to <id>]`
- Add auto-recovery: detect stalled or failed agents and attempt restart; escalate persistent issues to Master via Telegram
- Integrate with existing sessions_spawn API to actually launch agents on demand
- Log all routing decisions and outcomes to improve future matching (learning loop)

---
### [GREEN] Real-Time Web3 Alpha Feed & Whisper Network
**Category:** Research & Intelligence
**Risk Level:** green
**Confidence:** High

**Description:**
A high-signal crypto intelligence aggregator that monitors on-chain data (large wallet movements, DEX swaps, smart contract deployments), governance forums, and curated Twitter lists for alpha. Uses LLM to summarize and prioritize, pushing urgent alerts to Telegram with concise context and links. Maintains a daily digest of top insights and stores raw data for later analysis.

**Rationale:**
Master seeks trading edges and early opportunities in crypto. Manually scanning multiple sources is impossible at the speed markets move. This feed automates the noise-filtering and delivers only high-conviction signals in real-time, giving him a decisive information advantage. It's like having a 24/7 research assistant who only interrupts when something matters.

**Implementation Tasks:**
- Identify data sources: Ethereum/Gas, Arkham/Token Terminal for whale alerts, governance platforms (Snapshot, Tally), key influencer Twitter lists
- Build fetchers with websocket streams where possible (e.g., Ethereum events), fallback to polling
- Design scoring rubric: transaction size, wallet reputation (whale vs retail), novelty (new contract), velocity (sudden spike)
- Integrate LLM to generate one-sentence summaries with reason why it matters and potential trade ideas
- Implement Telegram push for high-priority alerts (score > threshold) with inline buttons to dismiss or dig deeper
- Generate daily digest at noon UTC: top 5 signals of the day, open on-chain positions to watch, upcoming governance votes
- Store all fetched data in JSONL for training future models or backtesting
- Add customization: Master can adjust alert sensitivity, filter by token or sector

---
### [GREEN] Backup Integrity Validator
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A post-backup verification script that runs after the Google Drive backup to ensure data integrity. It compares the number of files, checks file sizes, and verifies critical configuration files are present and not zero bytes. If discrepancies found, sends Telegram alert and suggests re-running backup.

**Rationale:**
Backups are only useful if they're complete and uncorrupted. Without integrity checks, a backup could silently fail or be incomplete, leaving us without a valid restore point. Automated validation gives confidence in the backup system and catches issues early.

**Implementation Tasks:**
- Create script `validate-backup.js` that runs after backup-to-gdrive.sh
- Use gog to list files in today's backup folder and compare with local file count
- Check critical files: openclaw.json, AGENTS.md, SOUL.md, MEMORY.md, credentials/ are present and >0 bytes
- Compute checksums (sha256) of critical files locally and compare with Drive versions (download and compare)
- Generate validation report with any mismatches or missing files
- If errors > threshold, send Telegram alert with summary and link to backup folder
- Modify cron to chain: backup.sh && validate-backup.js

---
### [GREEN] NFT Project Milestone Tracker
**Category:** Web3 & Crypto
**Risk Level:** green
**Confidence:** High

**Description:**
A Telegram-driven project tracker for the NFT collection launch. Master can create tasks, set statuses (todo/doing/done), assign priorities, and get daily/weekly summaries. Supports attachments (design mockups, contract links). Integrates with GitHub issues for technical tasks and sends nudges for overdue items.

**Rationale:**
The NFT project has been on the shelf due to lack of time and momentum. A lightweight, automated tracker keeps it top-of-mind, breaks it into manageable steps, and provides gentle accountability without mental overhead. It turns a dream project into an active one.

**Implementation Tasks:**
- Design data model: milestones, tasks (title, description, status, priority, due_date, dependencies)
- Build Telegram bot interface: /milestones list, /milestone add <title>, /milestone done <id>, /milestone remind
- Store data in SQLite under Project/nft-tracker/
- Generate weekly progress report (Markdown) with completed tasks, upcoming deadlines, and burndown chart
- Add integration: link to GitHub issues; when issue closed, auto-complete task
- Set up cron to send reminder every Monday morning with week's focus tasks
- Include shareable link to view full project board (static HTML generated on demand)

---
### [GREEN] Ethereum Gas Fee Optimizer
**Category:** Web3 & Crypto
**Risk Level:** green
**Confidence:** High

**Description:**
A service that monitors Ethereum gas prices via Etherscan or Alchemy API, predicts short-term trends using simple heuristics (e.g., time of day, network activity), and sends Telegram notifications when gas is low (good for transactions) or high (avoid). Can also queue non-urgent transactions for later execution manually.

**Rationale:**
Master engages with Web3 and DeFi. High gas fees can erode profits on trades or NFT mints. By timing transactions optimally, he saves significant ETH and improves yield.

**Implementation Tasks:**
- Set up data source: subscribe to gas price feed (Etherscan Gas Tracker API, Alchemy, or Infura)
- Build predictor: historical analysis + time-based patterns to forecast next hour gas
- Define thresholds: e.g., <20 gwei = good, >100 gwei = bad
- Create Telegram bot that sends hourly updates or alerts when gas crosses thresholds
- Add command /gas now to get current gas price and recommendation
- Implement transaction scheduler UI: manually queue transactions, then auto-send when gas below target (user-approves each before broadcast)
- Store daily gas statistics for later analysis

---
### [GREEN] Dating Interaction Assistant
**Category:** Hobbies & Interests
**Risk Level:** green
**Confidence:** High

**Description:**
A discreet Telegram bot that helps Master manage dating connections. Features: store contact info (name, where met, interests), set follow-up reminders, get conversation starter suggestions based on profile, and receive gentle nudges if no contact made in a while. All data stored locally; no external sharing.

**Rationale:**
Dating requires timely follow-ups and remembering details. Having a discreet assistant ensures he maintains connections, avoids forgetting important dates or info, and reduces the mental load of managing multiple conversations.

**Implementation Tasks:**
- Create a simple Telegram bot that responds to commands: /add <name> <where met> <notes>, /list, /followup <name> <date>, /suggest <name> (generates icebreaker from stored interests)
- Use SQLite to store contacts (id, name, source, interests, last_contact, next_followup)
- Implement reminder cron: daily check for due follow-ups and send Telegram notification
- Build suggestion engine: for a given contact, retrieve their interests and use LLM to generate a conversation starter (green - LLM only, no external comms beyond Telegram)
- Include a /delete command and privacy guarantee (all data on local machine only)
- Optionally integrate with calendar to block date nights
- Provide /stats to show number of active contacts and overdue follow-ups

---
### [GREEN] Smart Photo Library Organizer
**Category:** Hobbies & Interests
**Risk Level:** green
**Confidence:** High

**Description:**
An automated photo organizer that scans Master's photo directories (camera uploads, Google Photos exports, etc.), uses EXIF data and lightweight ML (face/object detection if available) to sort into a logical folder structure: `Photos/YYYY/MM/DD/`. Also generates a searchable index (by date, location, recognized objects/people) and tags images accordingly. Optionally, it can create monthly collage summaries.

**Rationale:**
Photography is a hobby but Master has limited time and often lets photos pile up unsorted. Automating the organization turns chaos into an instantly navigable library, making it easy to find and enjoy past shots without manual effort. It also preserves precious memories with proper backups.

**Implementation Tasks:**
- Scan source directories for image files (jpg, png, heic, raw)
- Extract EXIF metadata (datetime, GPS coordinates, camera model) using exiftool or piexif
- Create destination folder hierarchy based on capture date
- Move/copy files accordingly with naming: `YYYY-MM-DD_HH-MM-SS_<original>`
- Optionally integrate face detection (opencv) to create `People/` subfolders with named folders (after initial labeling)
- Build a simple SQLite index with tags (date, location, camera, people) for quick search
- Provide a Telegram command /photos recent to get last 5 photos as a collage
- Run as a daily cron job for new uploads

---
### [GREEN] Automated YouTube Research Summarizer
**Category:** Research & Intelligence
**Risk Level:** green
**Confidence:** High

**Description:**
A service that, given a YouTube channel or search query, periodically fetches latest videos, downloads transcripts (or audio -> Whisper), and generates concise summaries (3-5 bullet points) with key takeaways and links to relevant timestamps. Stores summaries in `Research/youtube/` organized by topic/channel.

**Rationale:**
Master uses YouTube heavily for learning but lacks time to watch every relevant video. This tool automates the consumption of educational content, turning hours of video into quick text summaries he can skim to decide what's worth deeper viewing. It multiplies his research efficiency.

**Implementation Tasks:**
- Set up YouTube Data API client to search for videos by query or list channel uploads
- For each new video, check if transcript available; if not, download audio and transcribe locally via Whisper (or use openai Whisper API)
- Use LLM to summarize transcript into key points, highlight actionable insights, and note any mentioned resources/links
- Store markdown summary in `Research/youtube/<channel_or_topic>/<video-id>.md` with frontmatter (title, author, date, url)
- Build a Telegram command `/yt latest <query>` to fetch and send most recent summary
- Schedule daily cron to check configured channels (list in config) and generate summaries for any new videos
- Add a simple web view (or markdown index) to browse all summaries

---
### [GREEN] Automated Receipt & Expense Tracker
**Category:** Lifestyle & Habits
**Risk Level:** green
**Confidence:** High

**Description:**
A skill that scans Gmail for receipts, parses key details (merchant, date, amount, category) using LLM or regex, and stores them in a local SQLite database. Generates weekly spending summaries with category breakdowns and alerts for unusual spikes. Supports manual entry via Telegram.

**Rationale:**
Master makes various purchases (personal, business) and loses track of spending. Automating receipt capture and categorization gives him real-time visibility into cash flow without manual data entry, helping him manage finances better and prepare for tax time.

**Implementation Tasks:**
- Configure gog Gmail search: receipts, invoices, confirmations from common merchants (Amazon, PayPal, etc.)
- Build parser: use LLM (OpenAI) to extract structured data from email bodies; fallback to regex patterns
- Categorize transactions using a simple taxonomy (Food, Transport, Entertainment, Business, etc.)
- Store in SQLite table: date, merchant, amount, category, source_email_id
- Create weekly digest (Telegram message) with total spending, top categories, and comparison to previous week
- Add command /expenses [date range] to query on demand
- Set up daily cron to process new receipts and archive processed emails with a label

---
### [GREEN] LLM Prompt Benchmarking & Cost-Performance Analyzer
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A tool that systematically tests different prompts and model configurations (temperature, max_tokens) across a set of representative tasks to measure cost (tokens) vs. quality (expert scoring or automated metrics). Produces a ranked guide: which model/temperature combo is best for each skill/task type, and estimates monthly cost impact.

**Rationale:**
Master's automation uses multiple LLM calls. Without benchmarking, we might be overpaying for high-end models for tasks that could use cheaper ones with negligible quality loss. This analyzer provides data-driven optimization choices to reduce AI spend.

**Implementation Tasks:**
- Define a benchmark suite: a set of typical prompts from each OpenClaw skill (e.g., email classification, content generation, summarization)
- Establish a quality metric: for many tasks can use automated evaluation (e.g., BLEU, ROUGE) or similarity to a reference output; for others, use LLM-as-judge
- Run each prompt with multiple models (stepfun, minimax, openrouter models) and parameter sets
- Record token counts, costs (using provider pricing), and quality scores
- Aggregate results into a recommendation matrix: for skill X, use model Y with temp Z for best tradeoff
- Generate a report (Markdown) with top recommendations and potential monthly savings
- Create a CLI `openclaw benchmark` to run the suite and update recommendations
- Store baseline results in `Research/llm-benchmarks/` for trend tracking
- Periodically (monthly) re-run to account for model price changes and new models

---
### [GREEN] OpenClaw Skill Performance Profiler
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A profiling service that instruments each skill execution to capture metrics: start/end timestamps, error status, exit code, resource usage (CPU, memory), and external API calls. Data is aggregated into a SQLite database with analytics. Provides a CLI (`openclaw profiler`) and Telegram reports showing slowest skills, failure rates, and usage frequency. Can alert on anomalies (sudden slowdown, repeated crashes) and suggest optimizations (caching, batching).

**Rationale:**
As the OpenClaw ecosystem grows, it becomes hard to spot performance bottlenecks and failing skills without manual log inspection. Automated profiling gives Master a clear, data-driven view of which skills deliver value and which need attention, ensuring the system remains efficient and reliable.

**Implementation Tasks:**
- Create a lightweight wrapper that skills can `require('./profiler')` and call `profiler.startTask()` and `profiler.endTask()` or use a decorator pattern
- Define metrics: task_id, skill_name, start_time, end_time, duration_ms, success (boolean), error_message, peak_memory_kb, cpu_time_ms, external_api_calls count
- Set up central SQLite database to store profiles; implement retention policy (e.g., keep 30 days)
- Build aggregation queries: average duration per skill, failure rate, total runtime, top memory consumers
- Create CLI command `openclaw profiler report` that outputs markdown table with top 10 slowest/failing skills
- Add Telegram integration: send weekly summary; also real-time alert if a skill fails >3 times in succession or duration spikes >200%
- Optionally integrate with existing OpenClaw logging to capture unhandled exceptions
- Document how to instrument new skills
---
### [GREEN] DeFi Protocol Risk Scanner
**Category:** Web3 & Crypto
**Risk Level:** green
**Confidence:** High

**Description:**
A service that monitors DeFi protocols (Uniswap, Aave, Compound, etc.) used by Master and generates a weekly risk score based on TVL, audit status, age, governance decentralization, and recent incident history. Pulls data from DeFi Llama, protocol subgraphs, and governance platforms. Sends Telegram alerts if any protocol's risk score drops below a threshold.

**Rationale:**
Master engages with DeFi for yield and trading. Protocol risk is often overlooked but can lead to catastrophic losses. This scanner provides a quick, data-driven health check of the protocols he uses, prompting him to withdraw funds or avoid new positions before a potential issue.

**Implementation Tasks:**
- Identify data sources: DeFi Llama API (TVL, chain), protocol audit databases (Trail of Bits, Quantstamp), Snapshot governance activity, incident trackers (Rekt.news)
- Design risk factors with weights: TVL stability (30%), audit coverage (25%), age/longevity (20%), governance participation (15%), incident history (10%)
- Build a fetcher that compiles these metrics for a configurable list of protocols
- Compute a normalized risk score (0-100) and trend direction
- Store weekly snapshots in Research/defi-risk/
- Send Telegram summary every Monday with top concerns and recommended actions
- Add command /defirisk <protocol> for on-demand lookup

---
### [GREEN] Crypto Tax Lot Calculator
**Category:** Trading & Investment
**Risk Level:** green
**Confidence:** High

**Description:**
A local tool that ingests transaction history from exchanges and blockchain (via CSV or APIs) and tracks cost basis using FIFO/LIFO/HIFO methods. Computes realized gains/losses per sale, generates tax report (CSV for TurboTax), and provides an estimate of current tax liability. Updates daily and can project year-end gains.

**Rationale:**
Master's trading activity across multiple exchanges and chains creates complex tax accounting. Manual calculation is error-prone and time-consuming. Automated tracking ensures accurate reporting, avoids penalties, and helps optimize tax strategy.

**Implementation Tasks:**
- Define data ingestion: support CSV export from Binance, Coinbase, and optional direct API read with read-only keys; also support Ethereum transaction logs via Etherscan API for DeFi trades
- Design normalized schema: timestamp, asset, quantity, price, fee, type (buy/sell/swap), source, tx_hash
- Implement cost basis algorithms: FIFO (default), LIFO, HIFO with configurable choice
- Compute capital gains/losses on each sell event, accounting for fees; track short-term vs long-term (hold >1 year)
- Generate reports: Yearly summary (total gains, tax liability), CSV for tax software, detailed transaction ledger
- Add Telegram commands: /tax estimate, /tax report <year>, /tax projection
- Secure storage for API keys using OpenClaw config (gitignored)
- Optionally integrate with portfolio tracker to auto-categorize transactions

---
### [GREEN] Voice Note Transcriber & Summarizer
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A Telegram bot skill that accepts voice notes, transcribes them using Whisper (local or API), then generates a concise summary using an LLM. Stores both transcript and summary for search. Allows commands to retrieve past notes. Great for capturing quick thoughts hands-free.

**Rationale:**
Master often has ideas or notes on the go. Speaking is faster than typing, but voice notes are not searchable. This skill automates transcription and summarization, turning fleeting thoughts into organized, retrievable text without any manual effort.

**Implementation Tasks:**
- Set up Telegram bot that receives voice messages (ogg/opus)
- Download audio and convert to wav if needed
- Transcribe using Whisper (either local binary or openai Whisper API)
- Use LLM to generate a 2-3 sentence summary highlighting key points
- Store in SQLite: timestamp, original file path, transcript, summary, tags (optional)
- Provide query commands: /notes recent, /notes search <query>, /note <id>
- Add ability to tag notes via command after transcription
- Optionally, implement archiving and export to markdown files
- Ensure all processing happens locally or with secure API keys (gitignored)

---
### [GREEN] Local Knowledge Base Search (RAG)
**Category:** OpenClaw Workflow and Automation
**Risk Level:** green
**Confidence:** High

**Description:**
A Retrieval-Augmented Generation (RAG) system that indexes all personal documents (MEMORY.md, daily notes, research files, PDFs) and allows natural language querying via Telegram or CLI. Uses embeddings (local model like all-MiniLM-L6-v2) and vector store (SQLite + sqlite-vss or file-based) to find relevant passages, then synthesizes an answer with an LLM, citing sources.

**Rationale:**
Master accumulates vast amounts of notes and research but can't recall everything instantly. A local RAG turns his entire knowledge base into a conversational assistant, providing quick answers with citations and saving time searching manually. It's like having a personal researcher who knows everything he's ever written down.

**Implementation Tasks:**
- Choose embedding model (sentence-transformers) and vector storage (sqlite-vss or simple file-based with cosine similarity)
- Build indexer that scans workspace: memory files, AGENTS.md, MEMORY.md, Research/, credentials/ docs (if allowed), and optionally PDFs via pdf-parse
- Create incremental indexing: on file changes, update vectors
- Implement query pipeline: embed query → search top K chunks → augment prompt with context → LLM generate answer with inline citations (e.g., [source.md L23])
- Expose via CLI: `openclaw ask "question"` and via Telegram bot: /ask <question>
- Add caching for frequent queries to reduce LLM costs
- Ensure index encryption or local-only storage for privacy
- Provide /reindex command to rebuild index manually

---
### [GREEN] Automated Trading Journal & Performance Analytics
**Category:** Trading & Investment
**Risk Level:** green
**Confidence:** High

**Description:**
A service that automatically imports trades from exchange APIs (Binance, Coinbase, etc.) and builds a comprehensive trading journal. It calculates key performance metrics: win rate, average win/loss, profit factor, Sharpe ratio, maximum drawdown. Generates daily/weekly reports with charts (equity curve, heatmap by hour/day). Highlights what's working (by strategy, time-of-day) and warns about deteriorating performance.

**Rationale:**
Manual trade journaling is inconsistent and often incomplete. Without rigorous tracking, Master cannot objectively evaluate his trading strategies or identify psychological biases. Automated analytics provide the feedback loop necessary to improve edge and become a consistently profitable trader.

**Implementation Tasks:**
- Connect to exchange APIs with read-only keys to fetch fill history (symbols, side, price, quantity, fees, timestamp)
- Normalize data into a unified trades table (exchange, asset, entry/exit, PnL)
- Compute metrics: total trades, win rate, avg win, avg loss, expectancy, Sharpe, max drawdown
- Build report generator: weekly markdown with performance summary, top 3 best/worst trades, time-of-day analysis
- Create simple web dashboard (static HTML) with interactive charts (using chart.js or quickchart)
- Set up Telegram command /trades report to fetch latest summary on demand
- Add alerts: if win rate drops below 45% over last 20 trades, or max drawdown >15%
- Secure storage for API keys using OpenClaw config

---
### [GREEN] Code Review Assistant for GitHub PRs
**Category:** Work & Career
**Risk Level:** green
**Confidence:** High

**Description:**
A GitHub App/Bot that automatically reviews pull requests in repositories Master owns. Uses LLM (Claude, Step) to analyze code changes, looking for bugs, security issues, code smells, and deviations from project conventions. Posts comments with line-specific suggestions. Prioritizes high-risk changes (auth, payments). Can be configured to block merge until review passes.

**Rationale:**
As a developer, Master wants to maintain high code quality but lacks time to review every PR thoroughly, especially in his own projects. An automated first-pass reviewer catches obvious mistakes early, enforces standards, and reduces the manual review burden. It scales his expertise across all his repositories.

**Implementation Tasks:**
- Build GitHub App using Probot or Smee.io for webhook handling (prerequest, issue comment events)
- Create prompt engineering for code review: feed diff, ask for issues with classification (critical, warning, info)
- Integrate with LLM API (OpenRouter) with cost tracking; use context-aware prompts referencing project-specific rules stored in repo (e.g., .codereview.yml)
- Post comments on PR with precise line references; include code snippets for suggested fixes
- Add configuration: minimum required AI score, auto-request changes, or just comment
- Implement caching to avoid re-reviewing unchanged files; rate limiting
- Provide a /review summary command in Telegram to see pending reviews
- Store review history and feedback to improve prompts over time

